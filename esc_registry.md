# iVendorz — ESC Registry (single source)

**Role:** Lead Product Designer + Frontend UI Engineer
**Status:** **DRAFT v0.3** — ESC Registry (non-authoritative companion; the single source for escalation handles)
**Date:** 2026-06-29
**Wave:** 0.3 — Governance Refactor (foundation)
**Companions:** all design-wave docs reference this; see [`shared_conventions.md`](docs/frontend/components/shared_conventions.md)

---

## 0. Purpose & rules

This is the **one place** every escalation (`[ESC-…]`) is defined. Other docs **reference the handle
only** (e.g. "Future: `ESC-7-API/upload`") and **never re-explain** it. The handles are **escalation
pointers, not architecture** — each names a gap in the frozen wired surface that must be resolved via
its **named channel (additive Doc-5x/Doc-3 patch, Board) — never locally** (Doc-7C §0.3; CLAUDE.md §11).
This registry **coins no contract** — it records what is *absent* and the agreed interim presentation.

Precedence: non-authoritative; on conflict the frozen corpus wins.

---

## 1. Registry

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-7-API-CATNAV`** | `list_categories` has **no Public projection** → public Industrial Category Explorer tree blocked | Render `search_catalog` facets only; counts via facet aggregation | Additive Doc-5D public-projection patch (Board) |
| **`ESC-7-API-PRODDETAIL`** ✅ **RESOLVED 2026-07-03** | `get_product` is User-only → **no anonymous product detail page** | **Ruling (`R-ESC7-PRODDETAIL-FREEZE`):** new Public query contract `marketplace.get_public_product_detail.v1` (BC-MKT-6) composes the canonical Product Detail Projection with the R9 non-disclosure collapse enforced once; `vendor_slug` withheld from the payload, `canonical_url` opaque/builder-only (never client-constructed). Canonical product URL law = `ADR-025_Marketplace_Public_URL_Law` — apex host `/marketplace/product/{name-slug}-{uuid}` (id-anchored, no vendor-host product route in v1); breadcrumb `primary_category_path` picked by deterministic rule (deepest → specialized → primary-level → lowest-UUID). Rate limit bound to registered POLICY `marketplace.public_read_rate_limit`. All three Annex-E instruments (E-1 contract+wire, E-2 POLICY registration, E-3 URL law) folded together 2026-07-03: `Doc-4D_PublicProductDetail_Patch_v1.0.3.md` + `Doc-5D_PublicProductDetail_Patch_v1.0.1.md` + `Doc-3_Policy_Key_Registration_Patch_v1.11_PublicReadRateLimit.md` + `ADR-025_Marketplace_Public_URL_Law.md`. **Unblocks FE-PUB-05** (interim modal-from-`search_catalog` retired at cutover). The FE Doc-7 leg (canonical-URL builder code, redirects, Host Resolution Matrix for products) is a separate future build step, not a further escalation gate. Full record: `governanceReviews/ESC-7-API-PRODDETAIL_Product_Detail_Architecture_Plan_v1.0_PROPOSAL.md` (`R-ESC7-PRODDETAIL-FREEZE`, Round 4 Final Resolution). | Human Architecture Board — ruled (owner, 2026-07-03) |
| **`ESC-7-API-ADS`** | ad reads are User-only → **no anonymous ads** | Ads not rendered on the Public surface | Additive Doc-5D patch (Board) |
| **`ESC-7-API-SIGNUP`** | No client-facing **`create_user` / signup-provision** contract in the wired surface — the user record + Personal Organization + Owner membership are materialized **out-of-band** by M1 lazy-provisioning (Doc-7E §2), not a coined wire contract | Auth entry via **Supabase Auth only**; the signup form is presentation-only and creates no account; provisioning runs on first authenticated request (`ensureProvisioned`, Doc-7C §3.2) | Additive Doc-5C / Doc-7E patch (Board) |
| **`ESC-7-API/upload`** | No client-facing **upload-grant** (signed-URL issuance) in the wired surface | Blobs to Supabase Storage; contract carries `file_ref` only | Additive Doc-5x/Doc-4B patch (Board) |
| **`ESC-7-API/export`** | No dedicated **bulk-export** contract | Export only user-readable (exclusion-applied) data; large export via create-then-poll | Additive Doc-5x patch (Board) |
| **`ESC-7-API/related`** | No **related-products / similar-suppliers** recommendation contract | Show same-category facets, labelled "Same category" — never "Recommended" | Additive Doc-5x patch / future M9 (Board) |
| **`ESC-7-API/stats`** | No **public marketplace-statistics** read | Omit, or show only contract-provided facet counts — never fabricated numbers | Additive Doc-5D patch (Board) |
| **`ESC-7-AI`** | A global conversational AI navigator/assistant beyond M9's wired `Doc-5K` advisory is absent | AI entry reserved "Coming Soon"; limited to `Doc-5K` non-recommending advisory | Additive Doc-5K patch / activation (Board) |
| **`ESC-IDN-DELEG-EXPIRY`** ✅ **RESOLVED 2026-07-09 (owner ruling)** | `reinstate_delegation_grant` pending on the Doc-2 §5.10 suspended-at-expiry boundary (held through W2-IDN-4: machine omitted the edge, sweep active-only, reinstate = scaffold throwing this handle) | Reinstate hidden/scaffold-gated until the ruling | **CLOSED — owner ruled 2026-07-09:** `suspended → expired` on window lapse; `expired` terminal per instance; reinstate ONLY for suspended non-expired; post-terminal delegation = NEW grant (new UUID + fresh audit). Instrument: `generatedDocs/Doc-2_Patch_v1.0.7.md` (Authority-Map-registered); record: `governanceReviews/BOARD-DECISION-IDN-DELEG-EXPIRY_v1.0.md`; realization (machine edge + sweep extension + real reinstate + tests) = **W2-IDN-6.5 (contract #25 un-gated)** |
| **`ESC-RFQ-POLICY`** / **`ESC-OPS-POLICY`** | Some idempotency dedup-window POLICY keys not yet registered | Use the named POLICY key once registered; never a UI literal | Additive Doc-3 §12.2 patch (Board) |
| **`ESC-MKT-SLUG-RESOLVE`** ✅ **RESOLVED 2026-07-11** | `marketplace.get_public_vendor_profile.v1` accepts only `vendor_profile_id`/`human_ref` — **no slug lookup mode** — but the already-shipped public FE route `app/(public)/vendors/[slug]/` (P-PUB-13) is keyed on the kebab-case marketing `slug` realized by `Doc-6D_VendorSlugSubdomain_Patch_v1.0.1` (ADR-024); the only frozen slug-adjacent mechanism (`vendor_slug_history`) resolves **retired** slugs for 301s, not a **live** slug → id lookup. Found during Wave-3 M2 pilot-slice scoping. | **Ruling:** new Public query contract `marketplace.resolve_vendor_slug.v1` (BC-MKT-6) resolves a `slug` to `{current, migrated, not_found}` — identifier resolution only, no vendor metadata. `slug` is accepted **only** by this contract (scope guard — `get_public_vendor_profile.v1` and every other Marketplace contract keep using `vendor_profile_id`/`human_ref` exclusively). Two-hop non-disclosure case (slug migrated, then vendor banned/unpublished) collapses to `not_found` per Invariant #11 (blacklist undetectable) — not a discretionary choice. No new POLICY key — adopts the existing `marketplace.public_read_rate_limit`. Both instruments folded together 2026-07-11: `Doc-4D_VendorSlugResolve_Patch_v1.0.4.md` + `Doc-5D_VendorSlugResolve_Patch_v1.0.2.md`. **Unblocks `W3-MKT-1`** (the M2 Wave-3 pilot vertical wiring `app/(public)/vendors/[slug]/` to a real backend read). Does not touch `[ESC-MKT-SUBDOMAIN-MIGRATE]` (the M8-mediated slug-migration write, still open) or `[ESC-7G-SLUG-MKT]` (custom-domain Doc-5D grounding, still open) — related but distinct channels. | Human Architecture Board — ruled (owner, 2026-07-11) |
| **`ESC-MKT-HUMANREF-ENUM`** 🟡 **OPEN — carried non-blocking (owner-ruled 2026-07-11)** | Team-6 W3-MKT-1 security review: `human_ref` is allocated by `core.allocate_human_ref` (Wave-0 M0 infra) as a plain per-year monotonic counter (`VENDOR-2026-000001`, `000002`… — "gap-tolerant, never-reused," verified against `prisma/migrations/20260627183528_core_init/migration.sql`) and is exposed as a public lookup key on the **already-frozen** `marketplace.get_public_vendor_profile.v1` (Doc-4D PassB Discovery, realized for the first time by W3-MKT-1). An anonymous caller walking sequential refs finds interior 404 gaps between live vendors — a probabilistic signal that a ref was allocated to a vendor now hidden (banned/suspended/soft-deleted), in tension with Invariant #11 ("blacklist undetectable"). **Not introduced by W3-MKT-1** — the allocator and the contract's human_ref lookup mode both predate this session; W3-MKT-1 only realizes an already-frozen contract for the first time. Same exposure applies to every other human_ref-keyed public read in the corpus (e.g. `get_public_product_detail.v1`), not vendor-specific. | Response bodies remain byte-identical for hidden-vs-absent (the frozen non-disclosure standard, CHK-8-024) — only the **sequence-position gap-analysis angle** is affected, not direct data disclosure. Accepted as a tracked, non-blocking gap for W3-MKT-1's merge (owner ruling, 2026-07-11) — mirrors how `[ESC-MKT-SUBDOMAIN-MIGRATE]`/`[ESC-7G-SLUG-MKT]` are carried elsewhere without blocking unrelated WPs. | Additive Doc-2/Doc-4B patch (allocation jitter/randomization) **or** Doc-4D/Doc-5D patch (drop `human_ref` as a public lookup key, UUID-only) — Human Architecture Board; program-wide impact, not scoped to M2 |
| **`ESC-MKT-RATELIMIT-ENFORCE`** 🟡 **OPEN — carried non-blocking (owner-ruled 2026-07-11)** | Team-6 W3-MKT-1 security review: `marketplace.public_read_rate_limit` (`Doc-3_Policy_Key_Registration_Patch_v1.11_PublicReadRateLimit`) is registered by **name only** — no numeric value, no enforcement mechanism (middleware/throttle) exists anywhere in the repo (verified: no `middleware.ts`, no rate-limit library, repo-wide). First shipped this way with `get_public_product_detail.v1` (folded 2026-07-03, `[ESC-7-API-PRODDETAIL]`); W3-MKT-1 (2026-07-11) adds two more anonymous endpoints on the same already-accepted posture — `resolve_vendor_slug.v1`'s slug keyspace is dictionary-guessable and `get_public_vendor_profile.v1`'s human_ref leg is sequentially enumerable (`[ESC-MKT-HUMANREF-ENUM]`), so absent throttling amplifies both. | No rate limiting on any Marketplace public read (pre-existing, program-wide — not a W3-MKT-1 regression). Accepted as a tracked, non-blocking gap for W3-MKT-1's merge (owner ruling, 2026-07-11); should land before real user-facing traffic, not gating individual WPs. | Separate future instrument (rate-limit middleware/mechanism + numeric POLICY value) — Engineering/Board; program-wide, not scoped to M2 |
| **`ESC-MKT-VENDORDIR-PROJECTION`** ✅ **RESOLVED 2026-07-11** | `marketplace.list_vendor_directory.v1` (already-frozen, Doc-4D PassB Discovery line 21/23) names no list-item field list (`items: list<public_projection>` — no field enumeration, unlike the parenthetical for `get_public_vendor_profile`), and its `filters` object is untyped while sibling `search_catalog` on the same line types the same filter group explicitly. Found during Wave-3 M2 second-slice scoping — a realization-layer gap, not a missing contract (unlike `[ESC-MKT-SLUG-RESOLVE]`). | **Ruling:** list item = the existing `PublicVendorProfileView` DTO (no new type) minus profile-experience/TrustIndicators; `filters` typed per the `search_catalog` sibling (`category_id: uuid`, 4 discrete geography fields, single-flag `capability` enum); sort `name`+`vendor_profile_id` tiebreaker; cursor pagination per the already-fully-specified Doc-5A §8, `page_size` bounded by the already-registered `marketplace.list_page_size_max` (no new POLICY key); exclusion-consistency gated by the existing `vendor-visibility.policy.ts` (no second predicate). Instrument: `Doc-5D_VendorDirectoryProjection_Patch_v1.0.3.md` (Doc-4D untouched — no new contract). | Human Architecture Board — ruled (owner, 2026-07-11) |
| **`ESC-MKT-VENDORDIR-SLUGFIELD`** ✅ **RESOLVED 2026-07-11** | `PublicVendorProfileView` (the DTO reused as the `list_vendor_directory.v1` list-item type) deliberately omits `slug` — for the single-vendor read the caller already has it from the URL segment, but a directory LIST has no per-row caller-known slug and no reverse id→slug read exists (`resolve_vendor_slug.v1` is slug→id only). Without it, the Vendor Directory and Search "Vendors" tab cards cannot construct a working link to each vendor's microsite. Found (and flagged, not silently added) during the `list_vendor_directory.v1` build. | **Ruling:** add `slug` as ONE additive **output** field on the list-item projection only (`VendorDirectoryListItem extends PublicVendorProfileView { slug }`) — `get_public_vendor_profile.v1`'s own DTO is untouched, and this does not violate `[ESC-MKT-SLUG-RESOLVE]`'s slug-as-input-only scope guard (that guard governs slug as a lookup *parameter*, not as a response field). Not a non-disclosure concern — a listed vendor's slug is, by construction, already its own live public URL segment. Instrument: `Doc-5D_VendorDirectorySlugField_Patch_v1.0.4.md` (PATCH-5D-VDS-01). | Human Architecture Board — ruled (owner, 2026-07-11) |
| **`ESC-RFQ-SLUG`** ✅ **RESOLVED 2026-07-10** | No dedicated routing-governance staff slug — Doc-4E §E6.5/§E6.6 + H.3 (`Doc-4E_PassB_Part3_v1.0_FROZEN.md`) and `Doc-5E_SERIES_FROZEN_v1.0.md:78` bound `staff_super_admin` as "nearest enumerated" (break-glass, never a standing team grant); raised by Doc-4E Pass-A Hard Review PA-05; blocked standing authorization for P-ADM-19/20/21 | **Ruling:** two slugs registered in Doc-2 §7 (read/write split) — `staff_can_view_routing` (routing/matching governance reads — monitor, never decide) + `staff_can_manage_routing` (`rfq.assist_routing.v1` + `rfq.manage_routing_rule.v1`, §3.6 forbidden-actions wall); `staff_super_admin` stays break-glass superset. Instrument: `generatedDocs/Doc-2_Patch_v1.0.8.md` (PATCH-D2-07, Authority-Map-registered). Full record: `governanceReviews/BOARD-DECISION-RFQ-SLUG_v1.0.md`. **Carried realization:** ✅ **catalog seed + Doc-6C count overlay (43→45 = 36+9) DONE @ W2-IDN-7** (`Doc-6C_Patch_v1.0.3` Authority-Map-registered; migration `20260710160000_identity_routing_slugs_seed`, +2 staff slugs on ZERO bundles per Inv#2; RV-0160 A+T6+B clean-gate close 2026-07-10 at `c562e7f`); **still carried:** Doc-4E/Doc-5E binding flips @ W4; P-ADM-19/20/21 wiring stays Wave-5 gated | Human Architecture Board — ruled (owner, 2026-07-10) |

### Billing / Monetization (M7 — W3-BILL-1…6, 2026-07-11)

| Handle | Gap | Interim | Channel |
|---|---|---|---|
| **`ESC-BILL-USAGE-ENTID`** ✅ **RESOLVED 2026-07-11 (owner ruling — Option B)** | **`usage_ledger.entitlement_id` NOT-NULL FK is unpopulatable by its sole writer — an internal Doc-4I conflict.** The schema mandates the column: **Doc-2 §10.8** (`billing.usage_ledger → entitlements`), **Doc-4I H.10** (field-source: `usage_ledger: →entitlements, …`), and **Doc-6I §3.3** (`entitlement_id uuid NOT NULL … REFERENCES billing.entitlements(id)`) all agree. But **Doc-4I §HB-3.1 `record_usage`** (the *only* writer; System metering) lists **no `entitlement_id` input** (inputs = org/acting_user/consuming_entity/quota_key/amount/period/source/source_event_id) and, after Hard-Review disposition **F4I-PB1-M1**, its Stage-7 states *"`quota_key` is free-form … not a foreign key and **no record-time entitlement lookup is specified**."* A NOT-NULL FK that the sole writer neither receives nor resolves is unimplementable. (F4I-PB1-M1 fixed `quota_key`; it never touched the `entitlement_id` FK.) Also NOTED (non-gating): §HB-3.1 requires `source_event_id` (Doc-4A §16 idempotency key) but Doc-2 §10.8/Doc-6I define **no such column** — reconciled locally as a framework idempotency key (H.8), not a persisted column; no ruling needed. Raised W3-BILL-6 (CLAUDE.md §11 Flag-and-Halt). | **`record_usage` DEFERRED — NOT built** (no local resolution: populating `entitlement_id` is a genuine design fork, each branch pre-judging the ruling). **BUILT around it (unblocked, no `entitlement_id` write):** the `usage_ledger` table (Doc-6I §3.3 verbatim — `entitlement_id NOT NULL FK` per all three frozen sources), `enforce_quota` (§HB-3.2), `get_usage` (§HB-3.3). Reads/enforcement never populate `entitlement_id`; tests seed rows with a valid FK. If the ruling relaxes the column (Opt C), the one migration line changes (unpushed). | **Owner/Board ruling.** Options: **(A)** `record_usage` resolves `entitlement_id` from `quota_key` (slug match); `REFERENCE` if unresolved — satisfies the FK but re-introduces the record-time entitlement dependency F4I-PB1-M1 removed (additive Doc-4I Stage-7/inputs patch). **(B)** add `entitlement_id` as a `record_usage` input supplied by the metering caller — additive Doc-4I §HB-3.1 input (leaves Stage-7 "no lookup" intact). **(C)** make `usage_ledger.entitlement_id` **nullable** — additive Doc-6I §3.3 patch (relaxes the FK across three frozen docs; weakest). Recommend **(B)** (honors both the FK and "no record-time lookup"; the metering signal's owner knows the metered entitlement). **CLOSED — owner ruled Option B 2026-07-11:** `record_usage` gains a required `entitlement_id` input supplied by the metering caller (no record-time lookup — Stage-7 retained). Instrument: `generatedDocs/Doc-4I_RecordUsageEntitlementId_Patch_v1.0.md` (additive §HB-3.1 input; Authority-Map-registered). `record_usage` build lands in a dedicated M7 slice; the W3-BILL-6 table + `enforce_quota` + `get_usage` already shipped. |
| **`ESC-BILL-RETIRE-VIS`** ✅ **RESOLVED 2026-07-11 (owner ruling)** | **Retired-plan read visibility — Doc-5I §4 vs Doc-6I §3.x RLS.** Doc-5I §4 (`get_plan`/`list_plans`) stated *"draft and retired plans are visible to all authenticated users"*, but Doc-6I realizes `retired` as **soft-delete** (Doc-2 §10.8 `plans` "SD = retire"; no `status` column — status is derived) and its `plans_public_read` RLS `USING (deleted_at IS NULL)` **hides retired rows from non-staff readers** (staff read them via `plans_admin`). Doc-4I §HB-1.4 (rank-0) is silent, so the divergence was between the two realization docs only. Raised W3-BILL-1 (CLAUDE.md §11 Flag-and-Halt). | **Ratified behavior (== the W3-BILL-1 implementation):** the reads scope to the **non-retired** catalog (`deleted_at IS NULL` — draft + active) for authenticated non-staff; a retired plan → `404` (get) / excluded (list); `status=retired` → empty page. Retired is visible to **staff/admin only** (via `plans_admin`; test-proven at the DB-role-switch backstop). `status` on the non-staff surface is only ever `draft`/`active`. | **CLOSED — owner ruled 2026-07-11 (Option a):** *"Retired plans are not visible to normal users. Active/Draft → authenticated users; Retired → staff/admin only. Doc-5I updated to match Doc-6I. No RLS change required."* Instrument: `generatedDocs/Doc-5I_RetiredVisibility_Patch_v1.0.md` (additive §4 prose correction; Authority-Map-registered). **Carried (not a gap):** the staff-facing retired-read WIRE path lands with DC-3 staff resolution (production `resolveStaffContext` fail-closed today) + the BC-BILL-1 Admin-writes slice — the non-staff security fence is enforced + test-proven now, no non-disclosure risk. |
| **`ESC-BILL-STATUS-MODEL`** ✅ **RESOLVED 2026-07-11 (owner ruling)** | **Plan status model — Doc-4I/5I vs Doc-6I/Doc-2.** Doc-4I §HB-1.1/§HB-1.1a + Doc-5I §4 treat `is_active` as a marketing-visibility bool **distinct from** `status`, with `activate_plan` driving `draft→active` *"no is_active coupling"* and `update_plan` = *"no status edge"* — implying a dedicated `status` field. But the frozen schema (Doc-2 §10.8 / Doc-6I §3.1.1) has **NO `status` column**: `plans = {name, billing_cycle, price, currency, is_active}` + soft-delete, and Doc-6I realizes *"draft→active = set is_active"* / *"SD = retire."* So `is_active` IS the discriminator; `status` is derived. Raised W3-BILL-2 (CLAUDE.md §11 Flag-and-Halt); gates `activate_plan`/`update_plan`. | **Ratified (Model B, == the W3-BILL-2 implementation):** `status` DERIVED (retired⟺soft-deleted, active⟺is_active, else draft); `create→is_active=false` (draft); `activate_plan→sets is_active=true`; `update_plan` status-neutral (does NOT touch is_active); `retire→soft-delete`. No schema change. | **CLOSED — owner ruled Model B ("derived — keep schema") 2026-07-11.** Instrument: `generatedDocs/Doc-4I_5I_PlanStatusModel_Patch_v1.0.md` (additive Doc-4I/5I prose correction; Authority-Map-registered). Same `is_active`-vs-`status` tension as `[ESC-BILL-RETIRE-VIS]`; both resolved to the frozen schema. No RLS/schema/contract-count change. |

### Vendor Workspace (Doc-7G design companion — Track 2/3, 2026-06-30)

Source: `docs/product/requirements/vendor_planning_and_design.md` (v0.9-rc, freeze WITHHELD) — detail in its §12/§13. Packets in
`governanceReviews/`: [`BOARD-PACKET-VENDOR-FE-BLOCKERS_v1.0.md`](governanceReviews/BOARD-PACKET-VENDOR-FE-BLOCKERS_v1.0.md)
(the 3 BLOCKERs) · [`API-GOV-INTAKE-VENDOR-FE_v1.0.md`](governanceReviews/API-GOV-INTAKE-VENDOR-FE_v1.0.md)
(contract questions). Freeze convened by [`BOARD-SPRINT-VENDOR-FE-FREEZE_v1.0.md`](governanceReviews/BOARD-SPRINT-VENDOR-FE-FREEZE_v1.0.md). 30-second landscape: [`DECISION-MATRIX-VENDOR-FE_v1.0.md`](governanceReviews/DECISION-MATRIX-VENDOR-FE_v1.0.md).

**Freeze-gating BLOCKERs — human Architecture Board (the only path to companion freeze):**

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-7G-SCORE-DISPLAY`** ✅ **RESOLVED 2026-07-03** | Doc-5G §5.3 ("band + display score", Public-Badge) vs companion DP4 "bands only" — re-read shows Invariant 6 (Master §4) is a cross-mutation firewall, **display-silent** | **Ruling:** Trust Score (band + numeric 0–100 + verification badges + "Last updated" + high-level contributing factors) may render on **any public-facing surface** — not confined to vendor self-view. Never display: internal weightings/formula, matching/fraud-risk/ranking scores, confidence coefficients, hidden penalties, competitor-relative percentile. **No corpus patch** — Doc-5G/Doc-4G already grant public/no-slug read. Trust Score composition = 4 categorical pillars (Identity & Verification, Operational Reliability, Reputation, Platform Compliance); **exact formula/weights stay open** under `[ESC-TRUST-POLICY]` (backend-implementation-time). Performance Score is **NOT** covered by this ruling — stays band-only pending a separate ruling. Full record: `BOARD-PACKET-VENDOR-FE-BLOCKERS_v1.0.md` §ESC-7G-SCORE-DISPLAY. | Human Architecture Board — ruled (BOARD-PACKET §SCORE-DISPLAY) |
| **`ESC-7B-TRUSTSCORE`** ✅ **RESOLVED 2026-07-03** | Frozen kit `trust-badge.score?:number` renders raw 0–100 — Invariant-6 footgun; **child of** SCORE-DISPLAY | **Ruling: Option 3** — `score` is now a sanctioned prop wherever Trust Score display is authorized (any public-facing surface, per the parent ruling); no kit change. Lint/test discipline retained, re-scoped to forbid `score` only in genuinely internal/governance-only contexts (staff tooling, matching/ranking/fraud surfaces) — never in vendor/buyer/public marketplace surfaces. Full record: `BOARD-PACKET-VENDOR-FE-BLOCKERS_v1.0.md` §ESC-7B-TRUSTSCORE. | Human Architecture Board — ruled |
| **`ESC-7G-A7`** ✅ **RESOLVED 2026-07-12** | Doc-7A R6 / Doc-7C SR3 mandate Hybrid "mount both" but specify neither the internal IA nor a `(vendor)` route group (design-introduced) | **Ruling (all 5 sub-decisions approved as recommended):** co-mounted, grouped-not-merged; Trust read-only, seam-tagged, terminal; `(vendor)` ratified as a non-routing layout group under `(app)` + single shared `(app)` shell layout (Option 1, no corpus patch — final URL prefix + Phase-2 topology wiring still to be executed); org-level groups shared via Account, not duplicated per surface; Hybrid mobile bottom-bar = Buying/RFQs/Selling/Trust. Full record: `BOARD-PACKET-A7-HYBRID-COMOUNT-REALIZATION_v0.1.md`. | Human Architecture Board — **ruled 2026-07-12** |

**Contract gaps — API Governance Board (§7 r5; NOT freeze-blocking; each ships a fallback):**

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-7-API` (vendor pipeline count)** | No frozen vendor received-only count read (vendor-leg reads are cursor lists only) | Non-numeric "view" links; no badges/tallies | Additive count read (owner M4, own-org `vendor_leads`-by-stage) — recommend decline for v1 |
| **`ESC-7-API` (participation)** | Derived Platform Participation only on out-of-wire `identity.get_organization.v1`; Doc-7A §3.7 bars client use | No Buyer/Vendor/Hybrid label rendered | Additive **wired** M1 participation read (recommend High) |
| **`ESC-7G-Q-DRAFT`** | No frozen quotation draft-write/read (quotation is created at submit) | Client-local "Saved on this device" autosave | Additive M3 draft contract OR accept client-local (recommend defer) — supersedes companion `[ESC-7G-A6]` |
| **`ESC-7G-ENG-01`** | `ops.get_engagement.v1` projects no `rfq_id` (the column exists, Doc-2 §10) | "[pending projection]" for the engagement→RFQ link | Additive M4 projection of `rfq_id` (recommend approve) |
| **`ESC-7G-ENG-02`** | No buyer-org display-name in engagement scope (UUID only) | Neutral label ("Buyer organization") | Additive M1/M4 name read (Low) |
| **`ESC-7G-ENG-03`** | No `list_engagement_documents` for the BC-OPS-2 record set (only single get; BC-OPS-4 lists rendered artifacts) | E3 per-kind document enumeration build-blocked | Additive M4 child-ref projection on `get_engagement` (recommend approve) |
| **`ESC-7G-LEAD-NOTE`** | No vendor-leg private lead-note contract (note slug is buyer-side BC-OPS-1) | Note-typed `ops.add_lead_activity.v1` (frozen, vendor-owned) | Decline (accept fallback); do **not** coin `add_lead_note` |
| **`ESC-7G-LEAD-REF`** | No `vendor_leads` human-ref scheme | Render no lead human-ref | Decline (coin no `LD-` prefix) or corpus-reconcile |

**Corpus reconciliation — human (frozen-vs-frozen / watch):**

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-7G-LEAD-MACHINE`** | Doc-2/4F/5F lead machine (`received→quoted→negotiation→won\|lost→follow_up`) vs Doc-4M (`new→contacted→qualified→converted\|disqualified`) | Companion binds the Doc-2/Doc-5F spelling (per-module authority) | Human corpus-reconciliation (Flag-and-Halt; not AI, not API-Gov) |
| **`ESC-7G-ENG-04`** | Doc-2 §8 IR-02/IR-03 emit-cardinality on versioned challans/WCC | UI assumes one event per record/issue | Watch item (Doc-2 §8) |

**Resolved during verification (recorded, no longer open):** `ESC-7G-LEAD-FILTER` — frozen `ops.list_leads.v1`
already accepts a `stage` filter (Doc-4F BC-OPS-3); `ESC-7G-Q-01` — bound to `rfq.get_rfq.v1` (grant-scoped);
`ESC-7G-Q-IDEMP` — deleted (idempotency already bound via Doc-4A §14.2). Carried on existing channels:
`[ESC-BILL-SLUG]`, `[ESC-MKT-AUDIT]`, `[ESC-OPS-AUDIT]`, `[ESC-OPS-SLUG]`, `[ESC-RFQ-POLICY]`.

---

### Classification & Matching Decisions (2026-07-01)

Source: `governanceReviews/CLASSIFICATION-DECISION-RECONCILIATION_v1.0.md` (+ `ADR-023` proposal + three
additive patch proposals + `MATCHING-ENGINE-RECONCILIATION_v1.0.md`). All human-Board gated.

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-MKT-VENDORTYPE`** | `vendor_type_preset` enumerates **no values**; net-new vendor "commercial capability" facets (brand/OEM-authorization) unmodeled | Define the value set as metadata (Manufacturer/Supplier/…); commercial-capability facets deferred | Additive `Doc-4D_VendorTypePreset_Values` patch (Board); net-new facets → future M2 patch |
| **`ESC-IDN-BUYERTYPE`** | No buyer-type/segmentation on the profile; Procurement Maturity + department/role/frequency/approval-authority unmodeled | Add `buyer_type` on `buyer_profiles` (Invariant-#2-safe); maturity/role/etc. reserved | Additive `Doc-2_BuyerType` patch (next Doc-2 version at fold — v1.0.5 was taken by the ADR-024 VendorSubdomainSlug fold 2026-07-03) + Doc-6C (Board); net-new fields → future M1 patch |
| **`ESC-CLASS-INDUSTRY`** | Industry taxonomy **not modeled** (supersedes the note below) | Recommend M2-owned 4-level Industry→Sector→Sub-Sector→Application, referenced by ID (Inv #7) | Human Architecture Board — module-ownership decision (`Doc-2_IndustryTaxonomy` patch — next Doc-2 version at fold) |
| **`ESC-RFQ-PROCCAT`** | Buyer "procurement category" (Mechanical/HVAC) vs the frozen category tree | **Reuse `marketplace.categories`**; coin no new entity | Confirm (no patch expected) — Board note |
| **`ESC-RFQ-MATCH-EVOLVE`** ✅ **RESOLVED 2026-07-10** | Weighted-engine evolution (policy layer; industry/business-type ranking; subscription-in-rank) | **Ruling:** ① firewall rejections CONFIRMED — subscription/plan never enters rank (`Doc-3 §11.8/§12.1`, Inv #10); trust/performance consumed as **bands via events**, never hand-weighted raw scores; ② configurable Matching Policy layer (Standard/Urgent/High-Value/OEM, **Phase-C emphasis only, gates fixed**) **ADOPTED IN PRINCIPLE** — immediate instrument: none required (existing governance confirmed); **deferred instrument: Doc-3 §12.2 policy-profile registration patch @ Wave-4 Definition of Ready**; ③ industry/business-type ranking stays DEFERRED pending Track-1 (`ESC-CLASS-INDUSTRY`/`ESC-MKT-VENDORTYPE`); ④ M9 stays advisory-only (Inv #12). Full record: `governanceReviews/BOARD-DECISION-RFQ-MATCH-EVOLVE_v1.0.md` | Human Architecture Board — ruled (owner, 2026-07-10) |

### Canonical Vendor Subdomain URLs (Board-ruled 2026-07-03)

Source: `governanceReviews/BOARD-PACKET-CANONICAL-VENDOR-SUBDOMAIN_v1.0.md` (completed decision record +
4-round Findings Adjudication annex). Decision: **ADR-024**; realized by the Doc-2 v1.0.5 linked patch set
(+ Doc-4D v1.0.2 · Doc-6D v1.0.1 · Doc-3 v1.10 · Doc-7D §11 [fold pending]) + FE milestone **FE-PUB-10**.

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-MKT-CANONICAL-URL`** ✅ **RESOLVED 2026-07-03** | Corpus silent on universal vendor subdomains, slug format/immutability/reserved labels, canonical-URL primacy, and redirects — the only subdomain mention (Master §8.4 item 7) is an example under the **entitled** custom-domain feature | **Ruling:** every APPROVED vendor receives one permanent **Platform-issued Vendor Subdomain** `https://{vendor-slug}.ivendorz.com/` — universal, free, never entitlement-gated (NOT a "custom domain"; `create_custom_domain` rejects `*.ivendorz.com`). Vendor Slug law FIXED (`^[a-z0-9]([a-z0-9-]*[a-z0-9])?$`, 3–40, ASCII-only, `xn--` rejected); reserved labels = POLICY key `marketplace.reserved_subdomain_labels` (platform-owned namespaces, never retroactive). Slug vendor-immutable; only M8-mediated migration (permanent 301; old slug never reused — `vendor_slug_history`, Inv #8; URL identity ≠ Vendor identity). **CHR** (Doc-2 v1.0.5 D2-04.3 — authoritative on any conflict): non-routable → ∅; `active` bound custom domain canonical (subdomain 301s; reverts on release); else the subdomain; **fail-closed**, cached derivations invalidated on transitions. Legacy `/vendors/[slug]/*` 301s to the CHR output (composes with ADR-022's 7-page IA); `seo.canonical` advisory; Host Resolution Matrix 404s byte-identical (Inv #11/CHK-7-040 extended to hosts); vendor hosts anonymous — auth cookies never `Domain=.ivendorz.com`; vendor URLs via the canonical URL builder only; locale = path segments (reserved). Full record: `BOARD-PACKET-CANONICAL-VENDOR-SUBDOMAIN_v1.0.md`. | Human Architecture Board — ruled (owner, 4-round CTO review, Final Resolution APPROVED 2026-07-03) |
| **`ESC-MKT-SUBDOMAIN-MIGRATE`** | No admin-mediated **slug-migration wire contract** on the frozen surface (Doc-4D v1.0.2 explicitly does not coin it; Doc-2 v1.0.5 defines only the business semantics + §9 action "slug migration (admin-mediated)") | Slugs are vendor-immutable; **no migration path is exposed anywhere** (no UI, no contract); `vendor_slug_history` stays empty until the contract lands | Additive Doc-4D/Doc-5D patch (API Governance Board / Board) |

### Document Management (FE-DOC intake, 2026-07-03)

Source: owner-directed FE-DOC track creation (WBS v1.2; owner decisions + 3 adjudication rounds
recorded in `governanceReviews/milestones/fe-doc-01-buyer-documents-hub/WORK-PACKAGE.md`). Packet:
[`BOARD-PACKET-DOCUMENT-MANAGEMENT_v1.0.md`](governanceReviews/BOARD-PACKET-DOCUMENT-MANAGEMENT_v1.0.md).
Corpus verification: every kind/feature below is **grep-verified absent** from the frozen corpus —
the frozen post-award document set is exactly `loi | po | challan | wcc` (+ `trade_invoices`,
`payment_records` as separate aggregates) and the template format enum is the fixed five
`challan | bill | letterhead | quotation | wcc` (Doc-4F §F5/§F7; Doc-2 §10.5).

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-OPS-DOC-MUSHOK`** | **Bangladesh-statutory VAT documents (Mushok forms, e.g. 6.3) are unmodeled** — zero corpus mention; no document kind, format, or finance-record subtype exists. Priority-flagged: statutory for the platform's home market | FE-DOC surfaces render **only the frozen kinds/formats**; no Mushok category, placeholder, or coined slug anywhere | Additive Doc-2 §2/§10.5 + Doc-4F §F5/§F7 patch (human Architecture Board) — packet item 2a |
| **`ESC-OPS-DOC-KINDS`** | Credit/debit notes, packing lists, sales orders, contracts/agreements are unmodeled as document kinds (sales order + contract overlap PO/LOI semantics; credit/debit notes are invoice adjustments; packing lists are delivery children) | No placeholder categories, no coined slugs/states; taxonomy shows frozen kinds only | Additive Doc-2 §10.5 (+§5.9) + Doc-4F patch per accepted item; credit/debit routed via BC-OPS-5 Finance-Records owner analysis (human Architecture Board) — packet item 2b |
| **`ESC-OPS-DOC-FEATURES`** | Digital signatures (no PKI/signature aggregate), expiry reminders (no post-award expiry or scheduled-reminder workflow), document favorites/pinning (M2 catalog favorites ≠ document favorites), document tags, document↔project linkage (no frozen link from M4 documents to M2 projects) | No affordance rendered for any of these; hub facets exclude "Project"; no tag/favorite UI | Per-feature channels: Doc-4B/e-sign patch · M6 Doc-5H notification-rule patch · owner-module analysis for favorites/tags/project-linkage (human Architecture Board) — packet item 2c |

### Buyer Vendor Directory & Vendor Discovery (owner intake, 2026-07-03)

Source: owner proposal + 4 review rounds (2026-07-03). Package:
`governanceReviews/BUYER-VENDOR-DIRECTORY-RECONCILIATION_v1.0.md` +
[`BOARD-PACKET-BUYER-VENDOR-DIRECTORY_v1.0.md`](governanceReviews/BOARD-PACKET-BUYER-VENDOR-DIRECTORY_v1.0.md)
(pre-read: `DECISION-MATRIX-BUYER-VENDOR-DIRECTORY_v1.0.md`) + 3 additive patch proposals +
`docs/product/requirements/BUYER_VENDOR_DIRECTORY_SPEC_v1.0.md` (non-authoritative). All human-Board gated.
Directory core / Smart-Upgrade linking / vendor types / permissions = **already frozen** (BC-OPS-1;
A-03/PATCH-05 link-not-merge; PATCH-02) — pointed, not escalated. Mushok remains
**`ESC-OPS-DOC-MUSHOK`** (above) — not re-registered here.

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-VENDIR-DISCOVERY`** | Anonymous cross-buyer "Discovered Vendor" aggregation **conflicts with frozen canon** (Inv #6 · Inv #11 · Master §6.4 "analytics, or inference" · §10.4); no module may own it (M9 forbidden by Inv #12). Flag-and-Halt | **No discovery surface, aggregate count, or "referenced by N buyers" signal renders anywhere**; conformant adjacency = M8 missing-vendor intake + Doc-3 §11.4 cell grid | Human Architecture Board — packet **R2** (a: reject · b: consent-based submit via M8 intake, additive · c: **rank-0** Master §4/§6.4/§16.2 patch, Annex A draft, human-only) |
| **`ESC-VENDIR-OFFPLATFORM`** | Private (unlinked) vendors cannot participate in RFQ/engagement flows — every frozen surface binds `vendor_profile_id` (Doc-4E B.3; `operations.engagements` A-02). Owner ruled: pursue buyer-private off-platform **recording** (no vendor side; recording-only per ADR-002; M3 untouched) | Directory renders "Record off-platform purchase" **only after the ruling**; UI never offers RFQ/quote to unlinked private vendors | Human Architecture Board — packet **R3** (Shape A: XOR party ref on frozen engagements · Shape B: parallel buyer-private M4 aggregate) → `Doc-4F/Doc-2_Vendor_Directory` additive patches |
| **`ESC-VENDIR-INVITE`** | Buyer-initiated invite-to-platform unmodeled — frozen seeding is admin-driven (`vendor_claim_records.source enum<excel\|admin\|registration>`); `missing_vendor_suggestions` has **no submit contract** (admin triage/close only) | "Pending Invitation" nav section + Invite button absent until ruled | Human Architecture Board — packet **R4** → `Doc-4D_or_4J_Buyer_Invite` additive patch (M2 source enum + M8 intake submit + M6 delivery; M4 link-back already frozen) |
| **`ESC-VENDIR-FIELDS`** | Field/enum basket: contact/address/registration fields (`details_jsonb` vs first-class), category refs on private records, logo ref, `link_suggestions.match_basis` +`bin`/+`domain`, buyer import/export (no slug/contract), private↔private **Merge** (corpus is link-not-merge; merge silent) | Form fields beyond the frozen set persist via `details_jsonb` (no coined columns); duplicate prompt offers **Archive duplicate** in place of Merge; no import/export affordance | Human Architecture Board — packet **R5** items 1–6 → `Doc-4F/Doc-2_Vendor_Directory` additive patches; OBS-1 (trade-license home) + OBS-2 (Doc-4F "ADR-003 link-not-merge" vs Compendium ADR-003/ADR-005 pointer) noted for errata |

### Journey Atlas (journeys/ intake, 2026-07-06)

Source: the journey/lifecycle companion program ([`JOURNEY_ATLAS.md`](docs/product/journeys/JOURNEY_ATLAS.md)
+ 9 domain files). Packet: [`BOARD-PACKET-JOURNEY-ATLAS_v1.0.md`](governanceReviews/BOARD-PACKET-JOURNEY-ATLAS_v1.0.md).
Both items **grep-verified absent** from the frozen corpus during authoring; neither journey is
authorable without coining. The vendor-lead state-machine conflict surfaced by the same authoring
pass is **already registered** as `ESC-7G-LEAD-MACHINE` (above) — not re-registered.

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-JRN-BUYER-VERIF`** | Buyer-side verification ("Trusted Buyer") is **unmodeled** — `verification_records` bind to the vendor's owning org (Doc-4G H.3/H.9a/H.10; subject/type enums fixed "do not extend"; sole event `VendorVerified`); `identity.organizations` carries a `verification_level` field (`unverified/verified/enhanced_verified`, Doc-2 §3) but **no buyer-facing verification contract, flow, or badge exists** | No buyer-verification journey authored (atlas §8 "not ratified — escalated"); no "Trusted Buyer" badge/flow rendered anywhere | Additive Doc-4G/Doc-4C patch (human Architecture Board) — product decision first |
| **`ESC-JRN-LEAD-DIST`** | Admin "lead distribution" is **unmodeled** — Doc-4J has no lead entity or function; `vendor_leads` are created **only** by seam M6-2 (`VendorInvited` at invitation `delivered`); M7 lead credits are a commercial balance, never an assignment instrument | No admin lead-assignment surface or journey; leads remain invitation-born only | Human Architecture Board (owner product decision) — would require Doc-4J + Doc-4F additive patches |
| **`ESC-JRN-TKT-MACHINE`** | Support-ticket machine variance (frozen-vs-frozen): **Doc-4H BC-COMM-4** contracts are strictly linear `open → in_progress → resolved → closed` ("a non-resolved ticket cannot be closed"; no reopen; "no state added; no transition added") while **Doc-4M §M5** consolidates extra edges (`resolved ⇄ in_progress` reopen; `open/in_progress → closed`). Same class as `ESC-7G-LEAD-MACHINE` | Journey J-TKT binds the **Doc-4H** machine (per-module authority); no reopen/admin-close affordance rendered | Human corpus-reconciliation (Flag-and-Halt; not AI, not API-Gov) |

### Buyer Frontend Contract Gaps (buyer_journey_v1.0 intake, 2026-07-06)

Source: [`buyer_journey_v1.0.md`](docs/product/journeys/buyer_journey_v1.0.md) Part C (owner-directed Board intake,
2026-07-06 — "resolve individually, never by modifying the journey document"). Packet:
[`BOARD-PACKET-BUYER-FE-CONTRACT-GAPS_v1.0.md`](governanceReviews/BOARD-PACKET-BUYER-FE-CONTRACT-GAPS_v1.0.md).
All six routes were **reserved at BX-04** with the absence recorded in each `page.tsx` header;
adjacencies checked and NOT re-registered: `ESC-7G-Q-DRAFT` (vendor quotation draft — different
concept), `ESC-7G-ENG-03` (per-engagement doc enumeration — different scope), and the two frozen
favorites forms (`marketplace.catalog_favorites`, `operations.vendor_favorites` — frozen, not
escalated). **Opening this intake does not imply Board acceptance; each handle remains unresolved
until individually ratified.**

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-BUY-QUOTES-LIST`** | No **cross-RFQ quotation list** read — quotations exist only per-RFQ (`list_quotations_for_rfq`) or per-doc (`get_quotation`); a buyer-org-wide "all received quotes" aggregate is absent | `/quotations` reserved via `ImplementationPendingView` (BX-04); no data fabricated | Additive Doc-5E patch (human Board) |
| **`ESC-BUY-PO-LIST`** | No **cross-engagement purchase-order list** read — the PO is per-engagement (P-BUY-21, `get_engagement_document`); an "all my POs / order history" roll-up is absent | `/purchase-orders` (`?status=active\|history` copy-only) reserved via `ImplementationPendingView` | Additive Doc-5F patch (human Board) |
| **`ESC-BUY-MSG-INBOX`** | No **unified cross-RFQ message inbox** read — M6 exposes only per-RFQ clarification threads (`manage_clarification`, P-BUY-16) | `/messages` reserved via `ImplementationPendingView`; messaging stays per-RFQ | Additive Doc-5H patch (human Board) |
| **`ESC-BUY-REPORTS`** | No **org-wide procurement reporting** reads — the only analytics-tagged buyer surface is the per-RFQ comparison statement (P-BUY-15); every figure must be a contract read (R7), so no report is renderable | `/reports` reserved via `ImplementationPendingView`; journey-KPI candidates travel with this intake (buyer_journey §A.6, proposed non-authoritative) | Additive Doc-5E/5F reporting reads (human Board) |
| **`ESC-BUY-SAVED-VENDORS`** | No **vendor-saving** read/concept — explicitly distinct from P-BUY-05 Favorites (product/category only, owner ruling FE-BUY-10) and from the buyer-private CRM (Inv #11, a different concept) | `/saved-vendors` reserved via `ImplementationPendingView`; neither existing surface repurposed under a mismatched label | Product decision → additive Doc-5D/5F patch (human Board) |
| **`ESC-BUY-SPEC-LIB`** | No **buyer-facing specification library** concept — a spec library exists only vendor-side (`/workspace/company/spec-library`, P-VND-09); a buyer sourcing-reference library is absent everywhere in the corpus | `/spec-library` reserved via `ImplementationPendingView`; the vendor page is never repurposed across the workspace boundary | Product decision → module-ownership ruling + additive patch (human Board) |

### Comparative Statement (CS) Document (owner redefinition, 2026-07-06)

Source: owner ruling 2026-07-06 splitting the buyer comparison surface into the interactive
**Compare Workspace** (frozen-backed) and the **Comparative Statement (CS)** official procurement
document — canonical standard frozen at `docs/product/requirements/COMPARE_SHEET_UX_FREEZE_v0.1.md`
(header v1.0). Packet: [`BOARD-PACKET-CS-DOCUMENT_v1.0.md`](governanceReviews/BOARD-PACKET-CS-DOCUMENT_v1.0.md);
sign-off record: `governanceReviews/COMPARE-SHEET-CS-REDEFINITION_v1.0.md`. Every gap below is
corpus-verified absent. **Binding while open:** all evaluative CS content is buyer-authored (R6 is
platform-scoped — Doc-3 §9.1 FIXED); the system computes arithmetic only. Presentation-only build
proceeds under WP-1 rules; nothing here blocks it.

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-CS-DOCKIND`** | CS as a persisted **pre-award document entity** + canonical route + M3/M4 ownership — no pre-award document kind exists (frozen set = `loi\|po\|challan\|wcc`; "Evaluation record" class explicitly NOT modeled, `rfq-workflow.md` §7). *"The Comparative Statement is a generated procurement document derived from the buyer's Workspace selection. It is not an independent business entity until the Board approves ESC-CS-DOCKIND."* | CS = ephemeral print view over the Workspace selection at the **temporary** route `/rfqs/[rfqId]/comparative-statement` (mock-era only; migrates on approval) | Human Architecture Board — packet R1 |
| **`ESC-CS-REF`** | Official `CS-` human-ref series — prefix registry is fixed (ORG/RFQ/QTN/INV/DOC); "a new prefix requires a Doc-4A patch… never invented here" (Doc-4B) | **"Draft Reference"** derived from the RFQ ref, marked *pending governance*; zero `CS-` strings anywhere; temporary mock-era label only, auto-transitions on approval | Additive Doc-4A prefix patch (Board) — packet R2 |
| **`ESC-CS-EXPORT`** | Platform PDF/Excel export generation — explicitly NOT modeled (`rfq-workflow.md` §7; ESC-OPS-DOC-* class); frozen doc-gen is M4 engagement docs over the fixed five-format enum only | Browser print / print-to-PDF (user-agent, allowed now); Excel affordance = honest gated stub | Board intake — packet R3 |
| **`ESC-CS-LINEITEMS`** | Structured line items (desc/spec/unit/qty/per-vendor unit price; brand/origin optional) — RFQ `content_jsonb` + quotation `price_breakdown_jsonb` are opaque; their **internal schemas are dev-doc scope** (Doc-4E PassB-Part4), so this is a dev-doc ratification, not a corpus patch | CS renders mock line items; real data waits on the ratified schema | Dev-doc schema ratification — packet R4 |
| **`ESC-CS-LETTERHEAD`** | Buyer letterhead/branding asset — buyer profile has no branding fields (branding is vendor-only); "letterhead" exists only as an M4 org-scoped template format | Mock letterhead in the CS render | Board intake (M1/M2 patch or M4 template reuse) — packet R5 |

### Vendor Quotation Workbench (owner functional spec intake, 2026-07-06)

Source: owner functional-requirements direction 2026-07-06 (interactive quotation submission —
amendable item rows, live pricing math, reusable condition sets, submission info). Every gap below is
verified against the frozen submit surface: `rfq.submit_quotation.v1` carries exactly the EIGHT frozen
request fields (`price_breakdown`, `delivery_terms`, `warranty_terms`, `spec_compliance_declaration`,
`attachments_refs`, `rfq_version_id`, `invitation_id`, `rfq_id`) and the jsonb internals are dev-doc
scope (Doc-4E PassB-Part4 — same class as `ESC-CS-LINEITEMS`). **Binding while open:** the buyer's
original RFQ content is never mutated; amendments are vendor-proposed display state only; nothing
below is serialized into a submit composition until its channel resolves.

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-QTN-AMEND`** | Vendor-proposed **item amendments** + the auto-generated "Amended item list" as a submitted artifact — no amended-item structure exists on the frozen submit surface; `price_breakdown` jsonb internals are dev-doc scope. **Extended 2026-07-07:** per-line vendor **"Not offered" decision + optional reason** (owner directive) — same jsonb dev-doc class; the buyer's RFQ line is never altered | Amend/undo is client-local UI state: amber-highlighted rows, original buyer values always retained and restorable, dedicated "Buyer specification amendments" summary with optional notes; **not-offered lines stay visible with a status badge + reason, excluded from all totals**; nothing persisted or submitted | Dev-doc `price_breakdown` schema ratification (+ additive Doc-4E patch if a separate amended-list request field is ratified) — human Board / API-Gov |
| **`ESC-QTN-TERMS-GROUPS`** | Structured commercial-condition **groups** (payment / general / exclusion / special notes) beyond the frozen three term jsonbs — no frozen landing field for the extra groups | Groups render as client-local rows; only delivery / warranty / compliance anchor to their frozen fields by exact name; the extra groups are excluded from any submit composition | Dev-doc terms-jsonb ratification + additive Doc-4E patch for net-new fields (human Board / API-Gov) |
| **`ESC-QTN-CONDITION-SETS`** | Reusable vendor **condition sets** (save / load / rename / update / delete, max 5 per vendor) — no vendor-owned condition-set aggregate exists anywhere; module ownership undecided | Sets are device-local only (browser storage) with an honest "on this device" note; no server persistence, no cross-device promise | Product decision → module-ownership ruling + additive patch (human Board) |
| **`ESC-QTN-SUBMIT-INFO`** | Submission **message / contact person / contact number / remarks** — none are among the eight frozen submit fields | Presentation inputs, client-local; excluded from any submit composition; honest integration note | Additive Doc-4E patch (human Board / API-Gov) |
| **`ESC-QTN-CURRENCY-LIST`** | **Supported quotation currency list** (owner-directed: BDT · USD · EUR · GBP · CNY) — the platform is multi-currency-ready (currency stored per value field, Doc-2 §0.4) but no supported-currency policy/enum exists; the list belongs to platform config, not a hardcode | The workbench currency select offers the owner-directed five as an interim display choice (single currency per quotation); default remains BDT | M8 POLICY key registration (additive config/Doc-4J patch, human Board) |
| **`ESC-QTN-REV-LABEL`** ✅ **WITHDRAWN 2026-07-07** | **Vendor-editable revision number** (earlier owner PRD) conflicted with the frozen quotation version model (`quotation_versions.version_no` system-assigned, monotonic, immutable — Doc-2 §5.5 / Doc-4M; Invariant 8). **Owner ruling 2026-07-07 retracted editability: revisions are system-managed and append-only** — the conflict is resolved by retraction, no patch needed | The editable label was removed. The authoring page shows a **read-only "Rev N"** derived from the frozen submitted-version count (`current_version_no`; Rev 0 before first submit), right-aligned in the pricing-section header | Closed — owner ruling 2026-07-07 (frozen model unchanged) |
| **`ESC-QTN-PDF-SHARE`** | **Auto-generated quotation PDF + share instruments** on submit (download, copy-link, email, WhatsApp, Teams, print; "store PDF in quotation history") — platform doc-gen for quotations is unmodeled (frozen doc-gen = M4 engagement docs over the fixed five-format enum only; same class as `ESC-CS-EXPORT`) | Document-style read-only preview dialog renders pre-submit; browser print / print-to-PDF (user-agent) is the allowed interim; no platform-PDF or share affordance fabricated; no post-submit success screen simulated | Board intake (doc-gen ownership + additive patch) |
| **`ESC-QTN-COMPLIANCE-OPT`** | Owner ruling 2026-07-07: the **specification compliance declaration is NOT mandatory** — conflicts with the frozen submit surface, where `spec_compliance_declaration` is a REQUIRED request field (Doc-4E; the server submit gate checks it as a frozen-required section) | The authoring UI no longer marks the field required (label reads "Optional"); the frozen server-side submit gate is UNCHANGED until the patch lands — flagged, never resolved locally | Additive Doc-4E patch making the field optional (human Board / API-Gov) |
| **`ESC-QTN-RFQ-ACTIONS`** | RFQ-side quick-action artifacts (owner 2026-07-07): generated **"Download RFQ" / "Download BOQ"** documents (no such generated artifacts are modeled — frozen doc-gen = M4 engagement docs over the fixed five-format enum; the granted spec documents are the only real download set) and **Watch/follow RFQ** (no watch concept exists on any frozen M3 surface) | Download RFQ / Download BOQ / Watch RFQ render as disabled affordances with honest titles; "Ask question" links to the real M6 clarifications thread; Print = user-agent print (allowed interim) | Board intake — doc-gen ownership + M3 watch-concept product decision (additive patches) |
| **`ESC-QTN-NEGOTIATION`** | **Buyer↔vendor negotiation loop** (owner PRD 2026-07-07: buyer revises RFQ after receiving offers → vendors re-notified → vendors revise offers → repeat until CS) — quotation revisions ARE frozen (immutable versions within `submitted`, no new quota) and `rfq_versions` exist, but a mid-flight buyer re-issue → re-notify cycle is **unverified against the frozen RFQ machine** (Doc-2 §5.4 / Doc-4M must be read verbatim; no lifecycle transition may be coined) | Vendor surfaces render only frozen facts (version-locked snapshot label, own revision history); no "revised RFQ received" affordance, no re-notify affordance, no negotiation-round counter anywhere until ruled | Human Architecture Board — verbatim Doc-2/Doc-4M reconciliation + M3 owner analysis (backend scope; never an FE decision) |
| **`ESC-IDN-DISPLAYNAME`** ✅ **RESOLVED 2026-07-09 (owner: Option A)** | **Doc-4C ↔ Doc-2/Doc-6C divergence (RV-0148 MINOR-4, 2026-07-09):** Doc-4C §C3's `get_user` projection (and §C4's `update_user_profile` wire) reference a `display_name` field that **no realized column carried** — Doc-2 §10.2 (line ~717) and Doc-6C `identity.users` both lacked it; the W2-IDN-3 `get_user` honestly omitted it | `get_user` projected realized columns only until the ruling; W2-IDN-6.1 was gated on this handle | **CLOSED — owner ruled Option A** (realize the column): `Doc-2_Patch_v1.0.6` + `Doc-6C_Patch_v1.0.2` (linked pair, Authority-Map-registered); semantics = optional user-chosen presentation name, personal data, never an identifier; forward-only migration + projection/wire completion land at **W2-IDN-6.1 (un-gated)**; decision record in `governanceReviews/BOARD-PACKET-IDN-DISPLAYNAME_v1.0.md` |
| **`ESC-IDN-4M-INDEX`** ✅ **RESOLVED 2026-07-09 (owner approved)** | **Doc-4M M5 index divergence from Doc-2 §5 (RV-0150 OBS-1, 2026-07-09):** frozen M5 rows carried phantom `claimed` org rows, `closed` (nonexistent; canonical `soft_deleted`), collapsed membership `invited→active` (losing `pending`), and omitted restore/expire edges — six verified defects. NOT frozen-vs-frozen: Doc-4M self-subordinates to Doc-2 §5 (:18/:34/:351) and prescribes index correction | W2-IDN-5 implemented per Doc-2 §5.1/§5.2 verbatim; no code was gated on this handle | **CLOSED — owner approved 2026-07-09** (conditions: editorial only · no semantic change · no new state names · no ownership change — honored). Instruments: packet `governanceReviews/BOARD-PACKET-IDN-4M-INDEX_v1.0.md` (Annex A) → executed `generatedDocs/Doc-4M_Patch_v1.0.1.md` (Authority-Map-registered). **Sibling delegation-row erratum (:166-168, `pending` vs §5.10 `draft`) verified + raised in the packet close record — v1.0.2 candidate awaiting one-word approval** |
| **`ESC-IDN-2FA-RECOVERY`** 🟠 **OPEN (proposed 2026-07-10, W2-IDN-6.1)** | **`recovery_method : enum : optional` (Doc-4C §C4 PassB:192) declares an enum with no source pointer and no value set anywhere in the corpus** — Doc-2 §10.2 `two_fa` checked, no value set found (raised in `governanceReviews/milestones/w2-idn-6.1/COMPLETION-REPORT.md` §8; **independently re-verified at RV-0152 duty E**: single corpus hit = PassB:192, itself violating Doc-4A §9.4's `enum(<source pointer>)` grammar — a genuine frozen-authoring gap) | **Fail-closed:** any supplied `recovery_method` value → VALIDATION reject (Doc-4A §9.4 escalate-never-coin); the field is unusable until the value set is registered; nothing coined, nothing dropped silently | Additive Doc-4C patch (Board) — enum value-set registration |
| **`ESC-IDN-PREF-KEYS`** 🟠 **OPEN (raised 2026-07-10, RV-0152 F1)** | **`preferences : object : optional : schema-validated keys only` (Doc-4C §C4 PassB:176; Validation Matrix "keys allowlisted" :179; "bounded schema, not arbitrary JSON" :186) requires a preference key schema that NO corpus source registers** — no frozen preference-key catalog exists (Review-A verified at RV-0152; same gap class as `ESC-IDN-2FA-RECOVERY`). W2-IDN-6.1 initially realized the field fail-open (RV-0152 F1, MAJOR) | **Fail-closed (RV-0152 F1 accepted remedy):** any supplied `preferences` → VALIDATION reject until the key schema is registered; the owner MAY alternatively ratify opaque carriage as the interim posture by ruling on this handle | Additive Doc-4C patch (Board) — preference key-schema registration |
| **`ESC-WIRE-FIELD-CASING`** ✅ **RESOLVED 2026-07-10 (owner: Option B)** — was FLAG-AND-HALT CLASS (raised RV-0153 F1 · program-level, NOT identity-only) | **Wire JSON `result`-payload property naming diverges from frozen Doc-5A §3 (Pass2:67 verbatim: property name = abstract field name "verbatim, in `snake_case` … No casing transformation … (no `camelCase`/`PascalCase` aliasing)")** — the ratified house shape since the Wave-1 D7 buyer-profile wire serializes camelCase result payloads (`organizationId`, `userId`, `delegationGrantId`, `pageInfo.hasMore`…), carried through the closed 6.1 faces and instantiated by 6.5's new faces; never adjudicated anywhere (review-log grep = zero). Request side + envelope keys (`reference_id`/`error_class`/`error_code`) CONFORM — the divergence is response `result` payloads only | Frozen-vs-ratified-house conflict — **never resolved locally**; both delivered surfaces stand as-built pending the ruling; decision packet `governanceReviews/BOARD-PACKET-WIRE-FIELD-CASING_v1.0.md` (Option A = conform-to-frozen envelope-edge serialization + bounded retrofit sweep · Option B = additive Doc-5A §3 deviation ratification · + the 6.5 gating disposition) | **CLOSED — owner ruled Option B 2026-07-10** (ratify the deviation): additive `generatedDocs/Doc-5A_Patch_v1.0.1.md` (§3.2, v1.0→v1.0.1) — response `result`-payload property NAMES serialize `camelCase` (the delivered house shape since D7); requests / envelope keys / enum-values / identifiers stay `snake_case` verbatim; Doc-4A abstract names untouched (§3.2 self-classified `[realization convention]`). Review-A PASS 0/0/0/0/1 (`governanceReviews/Doc-5A_Patch_v1.0.1_REVIEW-A.md`); Authority-Map-registered (Doc-5A → v1.0.1). RV-0153 F1 [MAJOR] **DISSOLVES**; **un-gated the W2-IDN-6.5 clean-gate close**. Zero code change (delivered surfaces conformant by definition). Human Architecture Board (owner) — Doc-5A channel |
| **`ESC-IDN-CTX-SUSPENDED-DOWNSTREAM`** 🟠 **OPEN (raised 2026-07-10, W2-IDN-6.6 Flag-and-Halt)** | **Completeness question: does switch-side §C8 suspended-org denial + per-operation BUSINESS checks fully cover OBS-B1, or is a downstream active-context suspended-org gate needed?** A blanket `resolveActiveOrg` org-status gate (the 6.6 packet's part-B) is **frozen-INCOMPATIBLE** — Doc-4C §C5 `soft_delete_organization` (PassB:283/287) REQUIRES an Owner to hold a **suspended** org as active-org context (the §5.1 `active\|suspended → soft_deleted` edge), and Doc-5C §3.3 (PassB:180) makes active-org resolution **membership-only, NOT org-status**; the packet's part-B gate empirically reddened the closed W2-IDN-6.2 "soft-delete from suspended → 200" test. Residual: a session whose active_org was set pre-suspension resolves the now-suspended org as active (membership-only §3.3) — enforcement is per-operation BUSINESS, not global. Raised in `governanceReviews/milestones/w2-idn-6.6/COMPLETION-REPORT.md`; **coordinator-owned: the 6.6 packet's part-B over-specified against the 6.2 close-carry that pre-flagged exactly this tension**; **RV-0158 INDEPENDENTLY CONFIRMED** (§C5:283/:287 + §5.1:472 + §3.3 Pass1:180 verbatim; part-B unmeetable without violating §C5). Residual = a PRE-EXISTING gap (a session/default whose active_org was adopted pre-suspension resolves membership-only; operations with no org_status BUSINESS leg pass check_permission for a suspended org) — NOT introduced by 6.6, NOT fully closed by per-operation checks in the general case | **As-built = frozen-faithful:** switch denies suspended (§C8 BUSINESS, live-path, proven); downstream resolution stays membership-only (§3.3, part-B reverted). No blanket gate coined. **OBS-B1 CORE discharged at the switch (RV-0158)** | Board — rule whether switch + per-operation enforcement SUFFICES (close the handle) or an operation-aware downstream gate needs an additive Doc-4C/Doc-5C patch (never a local coin) |
| **`ESC-IDN-INVITE-ACCOUNT`** 🟠 **OPEN (proposed 2026-07-10, W2-IDN-6.3)** | **Doc-4C §C6 `invite_member` invitee identifier vs the `memberships.user_id NOT NULL` realized column** — the frozen invite surface identifies an invitee by `email`, but a membership row requires a non-null `user_id`, leaving the no-existing-account invitee unrealizable (no pending-user / invitation-token entity is frozen; raised in `governanceReviews/milestones/w2-idn-6.3/COMPLETION-REPORT.md` §8 call 1; **independently CONFIRMED at RV-0156 duty D** — §C6 PassB:348 email invitee vs Doc-6C §3.3:152 `user_id NOT NULL`; no frozen token/pending-user entity) | **Fail-closed:** invite of an email with no existing user → in-register VALIDATION 400 (the RV-0152 F1 posture); existing-user invite works; nothing coined | Additive Doc-4C/Doc-2 patch (Board) — no-account-invitee model (pending-user or invitation-token entity) |
| **`ESC-IDN-ORG-PROFILE-FIELDS`** 🟢 **FORMALLY DEFERRED 2026-07-10 (owner ruled: realize-vs-defer → defer + carry)** | **Doc-4C §C5 organization-profile wire fields with NO frozen Doc-2/Doc-6C column** (`org_type` · `address` · `contact_info` · `brand_assets_ref`) — the `ESC-IDN-DISPLAYNAME` gap class recurring on the org surface (raised in `governanceReviews/milestones/w2-idn-6.2/COMPLETION-REPORT.md` §8; **independently VERIFIED at RV-0155 duty E** — Doc-2 §10.2:718 org columns + patches v1.0.2/3 + Prisma model all lack the four; §C5 PassB:241-243/:257 carries them on the wire) | **Fail-closed:** supplied values → VALIDATION reject; NO migration authored (Invariant 8 / escalate-never-coin); fields unusable until the column set is ruled | Additive Doc-2/Doc-6C patch pair — **owner ruled FORMAL DEFER 2026-07-10** (the same ruling covers the §C11 workflow-settings deferred pair `default_routing_mode`/`buyer_courtesy_options`, RV-0159 F1): the **fail-closed reject-on-write / null-on-read posture is RATIFIED** as the standing interim; these fields are contract-declared but schema-pending, and realization (the additive Doc-2/Doc-6C patch + forward-only migration) is **CARRIED to Wave-4** (where the M3 routing consumers live). **No longer blocks the M1 module close** — an explicit, reviewed gap-record, not a silent contract↔schema divergence. Precedent: `ESC-IDN-DISPLAYNAME` Option-A (deferred → later realization) |
| **`ESC-IDN-LIST-PAGESIZE`** 🟠 **OPEN (proposed 2026-07-10, W2-IDN-6.5)** | **List-read pagination bounds (page-size default/max for `list_delegation_grants.v1` and future identity list wires) have no registered POLICY key or corpus value** — Doc-3 v1.9 §Notes pre-names exactly this escalation (raised in `governanceReviews/milestones/w2-idn-6.5/COMPLETION-REPORT.md` §8 judgment call 15; independent Review-A re-verification pending at RV-0153) | **Fail-closed per report §8 call 15** — no coined default; bounds enforced conservatively pending registration; nothing dropped silently | Doc-3 POLICY key registration (additive, Board) |
| **`ESC-IDN-SLUGCOUNT`** ✅ **RESOLVED 2026-07-09 (owner: Option A)** | **Frozen-vs-frozen permission-slug count mismatch (W2-IDN-2 Flag-and-Halt, 2026-07-09):** Doc-2 §7 (base text through the v1.0.5 patch chain) **enumerates 43 distinct slugs (36 tenant + 7 staff)**; Doc-6C §5.1 + Doc-6C Pass-3 CHK-6-062 + `backend_execution_playbook.md` §5 all **assert "45 (38 tenant + 7 staff)"** — none enumerates the 38; all cite Doc-2 §7 as the exhaustive list, which does not substantiate 45. Patch chain v1.0.3/4/5, PATCH-10, and Master Architecture §13 ruled out as sources of the missing 2. Evidence: `governanceReviews/milestones/w2-idn-2/HANDOFF-NOTE.md` §3/§4 + Board packet `governanceReviews/BOARD-PACKET-IDN-SLUGCOUNT_v1.0.md` | **W2-IDN-2 SUSPENDED at `c21c38f`** — zero seed rows written; nothing coined; even the enumerated 43 are NOT seeded until the ruling | **CLOSED — owner ruled Option A** (43 = truth; propagated counting error): `generatedDocs/Doc-6C_Patch_v1.0.1.md` (additive, registered in 00_AUTHORITY_MAP) + playbook/build-plan sync; W2-IDN-2 resumed seeding the enumerated 43; decision record in `governanceReviews/BOARD-PACKET-IDN-SLUGCOUNT_v1.0.md` |

### Communication (M6 · W3-COMM-1 support-ticket pilot intake, 2026-07-11)

Source: W3-COMM-1 (M6 BC-COMM-4 Support Communications pilot slice; branch `wave/3-communication`). Review
lanes: Review-A PASS · Review-B + Team-6 (Doc-8 8C/8D/8E; §13 gate BLOCKER=MAJOR=MINOR=0 after fixes). Board
close + commit authorized by owner 2026-07-11.

| Handle | Scope / gap | Interim presentation | Channel |
|---|---|---|---|
| **`ESC-COMM-AUDIT`** ✅ **RESOLVED 2026-07-11 (owner: Path A)** | Doc-2 §9 enumerated **no Communication audit domain** → the first M6 (support-ticket) audited write had no canonical §9 action (frozen marker on Doc-4H §H6/§H7, Doc-5H §7.5, Doc-6H §8) | **CLOSED — owner ruled Path A** (additive §9 + serialization, human-approved, D7 precedent): linked pair `generatedDocs/Doc-2_Patch_v1.0.9.md` (new §9 **Communication** domain — 4 business actions: create / status change / message append / close) + `generatedDocs/Doc-4H_SupportTicketAuditToken_Patch_v1.0.md` (tokens `support_ticket_created`/`_status_changed`/`_message_appended`/`_closed` + entity_type + old/new → exported `SupportTicketAuditAction.*`); Authority-Map-registered; realization = W3-COMM-1. **Scope = BC-COMM-4 only**; BC-COMM-1/2/3 `[ESC-COMM-AUDIT]` stay interim (future additive appends to the same §9 Communication row) | Human Architecture Board — ruled (owner, 2026-07-11) |
| **`ESC-COMM-STAFF-AUTHZ`** 🟠 **OPEN (carried 2026-07-11, W3-COMM-1)** | The BC-COMM-4 staff leg cannot **hard-gate** the specific `staff_can_support` slug yet — `check_permission` has no staff-space resolution and no DC-3 staff roster exists (staff basis = `resolveStaffContext` actor-type determination, fail-closed null in prod; Doc-5C §3.2). Raised by Team-6 (MAJOR) / Review-B (MIN-1): the staff leg admitted any resolved staff principal (`staff_can_support` was dead code); adjudicated MAJOR, resolved Option A (route-handler-scoped, owner 2026-07-11 — no shared-primitive change) | **As-built = fail-closed + precedent-faithful** (mirrors the ratified identity DC-3 posture, `set_user_account_status`): an advisory `staff_can_support` authority hook is invoked **live** on both staff legs (mutation + read) referencing `STAFF_CAN_SUPPORT_SLUG` + ⚠ DC-3 MAINTAINER WARNING naming the slug; the staff surface is **live-but-unreachable** in prod (`resolveStaffContext`→null). The specific-slug **hard gate** lands when the DC-3 staff roster + `check_permission` staff-space resolution exist | Roster-time enforcement — DC-3 staff channel + additive `check_permission` staff-space resolution (Board; Wave-5 / M8-adjacent) |
| **`ESC-CORE-OUTBOX-MECH`** ✅ **RESOLVED 2026-07-12 (owner ruling — Option A)** | Two Wave-3 modules independently realized the **M0-owned** `core.write_outbox_event.v1` (Doc-4B §Write-Outbox-Event / §B10) divergently: M5/trust (`31b997d`) as a **no-SD** non-`RETURNING` caller-context insert (the `append_audit_record` twin's realization); M7/billing (`b8e8518`) as a **`SECURITY DEFINER`** function. Conflict: the folded, human-approved `Doc-4B_AuditAppendRLS_Patch_v1.0.1` (R-c) rejects `SECURITY DEFINER` for the audit twin. Raised as a Flag-and-Halt at the Wave-3 exit gate (CLAUDE.md §11); the §A10 primitive trio is split on SD (`allocate_human_ref` IS SD), so binding was reserved for the Board. | **CLOSED — owner ruled Option A 2026-07-12** (Q1=Yes — the audit no-SD posture **binds** the outbox twin, normative for all M0 append primitives; Q2=Option A retain no-SD; Q3=conform to the frozen contract, do not expand the surface): canonical = trust's no-SD primitive (`31b997d`); **`WriteOutboxEventResult` dropped entirely** — the frozen contract declares `Response: none` (Doc-4A §21.5 carve-out) → the primitive returns **void**; `core_outbox_write_failed` kept; `payload: Record<string,unknown>`. M7's `SECURITY DEFINER` function + its `20260711180000_core_write_outbox_event` migration **withdrawn**; billing emitters re-pointed to `@/modules/core/contracts`. The optional Doc-6B tenant-INSERT policy was confirmed **NOT needed** — the app path runs on the privileged (RLS-bypassing) connection (proven by the passing `purchase_subscription` flow; 806/806 suite on ephemeral PG). No new frozen-corpus patch (Option A = conformance to the existing contract; RLS backstop unchanged). Realized at the Wave-3 exit gate (`wave/3-integration`). Record: `governanceReviews/BOARD-PACKET-W3-CORE-OUTBOX-MECHANISM_v1.0.md`. | Human Architecture Board — ruled (owner, 2026-07-12) |

### Known non-ESC gaps (recorded, not escalations)
- **Industry taxonomy** — now tracked as **`ESC-CLASS-INDUSTRY`** above (was: "not modeled"); the
  `Doc-2_IndustryTaxonomy` patch proposal recommends an M2-owned model for Board ratification.
- **Brand / Standard / Manufacturer taxonomies** remain **not modeled** in the frozen corpus.
  Navigation may *reference* them as wayfinding dimensions but **coins no data model**; introducing any
  is a module-ownership / architecture decision (escalate), not an IA/UX decision. See `IA §5.3`, `GL`.

---

## 2. How docs cite ESC

- A page/section/template names the handle in its `Future:` or a delta line — **no explanation**.
- This registry is the only place gap + interim + channel are described.
- If a new gap is discovered, **add a row here** (record the absence + interim + channel); **do not
  invent a contract** anywhere.

---

*Non-authoritative. Conforms upward; coins nothing. Each handle resolves only via its named channel,
never locally. On any conflict the frozen document wins.*
