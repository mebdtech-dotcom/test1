# Team-2 Queue — Buyer

**Owns:** `P-BUY-*` (Doc-7F · Doc-5E/5F/5D) · `P-DOC-01` (buyer leg of the FE-DOC cross-workspace
Documents track, WBS v1.2). Titles + bindings are the **source record** in
[`page_inventory.md`](../page_inventory.md). Work highest-priority `Ready` first. CRM pages
(`P-BUY-26/27`) are **buyer-private** (Invariant #11) — never leak, blacklist undetectable.

> **Milestone layer (FE-PM v1.0, 2026-07-02):** pages in this file are grouped into `FE-*`
> milestones in [`fe-program-wbs.md`](fe-program-wbs.md); pull work from your milestone queue via
> [`current-focus.md`](current-focus.md) / [`execution-board.md`](execution-board.md). This file
> remains the **page-level source record**; milestone status is derived from these rows.

| ID | Title | Priority | Dependency | Status | Notes |
|---|---|---|---|---|---|
| P-BUY-01 | Buyer dashboard | P1 | Ready | 🟩 Built | refinement item (owner-named). **FE-BUY-08 enhancement CLOSED** (RV-0113, `d501345`): Engagement pipeline widget added alongside the BX-01 Sourcing pipeline; `PipelineCard` promotion candidate registered (2 instances, watch for a 3rd). **BX-03 enhancement CLOSED** (RV-0133, `6316763`, owner-direct edit + Team-2 verification pass): `WelcomeBand` (single shared `BUYER_IDENTITY_SEED`, no second fabricated identity — Doc-7C SR3 PARKED), `KpiStatCard` icon/tone treatment (decorative only, StatusChip tone vocabulary, buyer-scoped not a new kit primitive — `kpi-stat-card.tsx`'s own header comment), topbar search shortcut (`ShellViewModel.search`, optional/additive field — plain `Link` to the real `/discover` route, never a fabricated live-search box, GI-12). **BX-04 CLOSED** (RV-0134): the whole Buyer left-nav re-grouped into the canonical IA — see BX-04 row below + `(buyer)/_components/buyer-nav-model.ts`; 6 new "Implementation Pending" routes reserved (`/quotations`, `/quotations/compare`, `/purchase-orders`, `/saved-vendors`, `/spec-library`, `/messages`, `/reports`) — presentation-shell-only, NOT new frozen `P-BUY-*` IDs (no Board mint requested), each page's own header comment states the missing contract |
| P-BUY-02 | Discover vendors | P1 | Ready | ✅ Approved | RV-0048 PASS (Team-4). Public projection only (Doc-4D §B.3); **trust=binary Verified only ([ESC-7G-SCORE-DISPLAY], no score/band, M5 owns); capability=4-flag matrix (Inv#1); discovery≠matching (DD-2, contract order, never re-ranked); non-disclosure clean (blacklist byte-identical §7.5); cursor.** Search box contract-legit (`search_catalog.query`). **`[PLAT-P7]` frozen-kit fix Team-4-verified (OFF-chip 5.12:1 AA; diff = sanctioned scope only; all VendorCard consumers benefit, P-PUB-12/16 stay ✅); PLAT-P7 CLOSED.** |
| P-BUY-03 | Vendor directory (in-app) | P2 | Ready | ♻ Superseded | **Owner-ruled 2026-07-03 (FE-BUY-10): "reuse existing surfaces."** `/discover` (P-BUY-02, ✅ Approved) IS the buyer-facing directory — no separate in-app directory route built |
| P-BUY-04 | Vendor profile (in-app) | P2 | Ready | ✅ Complete (link-out) | **Owner-ruled 2026-07-03 (FE-BUY-10): "reuse existing surfaces."** No in-app profile route; each `VendorCard` on `/discover` links out to the public microsite `/vendors/[slug]` (P-PUB-13) instead. `discover-view.tsx` href changed `/discover/${slug}` (dead forward-ref, P-BUY-04 never built) → `/vendors/${slug}`; `MOCK_VENDORS` in `page.tsx` re-aligned field-for-field with the public discovery seed (`app/(public)/_components/discovery/seed.ts` `VENDORS`) so every slug resolves instead of 404ing against a second, divergent fictional catalog — both surfaces already declare they'll bind the same M2 public reads at Wave-4 wiring, so aligned mock content previews that, it doesn't invent a new coupling |
| P-BUY-05 | Favorites | P2 | Ready | 🅿 Held (scope confirmed) | **Owner-ruled 2026-07-03 (FE-BUY-10): "confirm scope, hold the build."** Scope CONFIRMED product/category only per the frozen `marketplace.catalog_favorites` contract (Doc-4D §D7.5, `target_type = product\|category`; vendor favorites are the separate, already-shipped `operations.vendor_favorites` CRM surface, FE-BUY-09). Build stays held — `list_catalog_favorites` still projects no target display name, so the page can't render meaningfully until a target-resolution read exists (ESC-class, same family as `ESC-7-API-PRODDETAIL`). Not in scope for FE-BUY-10 |
| P-BUY-06 | RFQ list | P1 | Ready | 🟩 Built | cursor pagination |
| P-BUY-07 | RFQ create wizard | P1 | Ready | 🟩 Built | resumable draft |
| P-BUY-08 | RFQ detail — overview | P1 | Ready | 🟩 Built | status from Doc-4M |
| P-BUY-09 | RFQ detail — quotations | P1 | Ready | 🟩 Built | visibility-gated |
| P-BUY-10 | RFQ detail — activity | P2 | Ready | ✅ Approved | RV-0056 PASS (Team-4). **DEFERRAL/EXCLUSION INVISIBLE (Inv#11/Doc-3 §4.2/Doc-4E B.7 FIXED)** — Activity tab feeds only `data.lifecycle` (M0 audit, Inv#8) to shared `ActivityTimeline` (infers nothing); not-invited/deferred vendors never shown + explicit rendered note; grep-confirmed no leak. R6 (buyer never dispatches). Additive tab realization in P-BUY-08 (Overview/Quotations unchanged); no coined field. Presentation-only |
| P-BUY-11 | RFQ version history | P2 | Ready | ✅ Approved | RV-0061 PASS (Team-4). `get_rfq_version.v1` §E4.8 verbatim (21.3 Query, User(buyer)/internal-service, BC-1); projection {rfq_id, human_ref, state, current_version_no, scope-appropriate version content}; two DISTINCT slugs `can_view_rfq`/`can_view_all_rfqs`. **NO per-version author/timestamp coined** (§E4.8 enumerates none; `rfq_versions.created_by` in DB but not projection → omit-not-coin); content fields map-by-intent to `content_jsonb` (dev-doc schema), all optional, "bind at wiring". **READ-ONLY (Inv#8 immutable/append-only) — no edit/rollback.** version_no order = intrinsic not a re-rank (GI-04); diff pure presentation over 2 disclosed versions, recommends no version (R6); markers text badges not color-only (GI-06). notFound byte-identical, breadcrumb no leaf ref (Inv#11/§7.5). Currency-driven Money; `?v=` server-guarded selector (not a dev harness); opaque route id (Inv#5). Reuses shell/shared/kit |
| P-BUY-12 | Internal approval | P1 | Ready | ✅ Approved | RV-0052 PASS (Team-4). **NO AUTO-APPROVE rendered (Doc-3 §1.2 FIXED)**; `approve_rfq`→submitted / `reject_internal_rfq`→draft + mandatory reason (§E4.4), parked/disabled, gated `can_approve_rfq`; approval-chain = Identity-owned (consumed, not coined); own-org queue (`list_rfqs` state=pending_internal_approval); genuine-empty; cursor/contract-order; currency-driven Money; opaque ids→/rfqs; stickyFirstColumn pre-existing. Model management page |
| P-BUY-13 | Routing log / invitations | P2 | Ready | ✅ Approved | RV-0064 PASS (Team-4) — **NON-DISCLOSURE surface, exemplary.** invitation projection {state, delivered_at, responded_at} NO vendor field (§E6.7:346) → screen-spec "vendor" column DROPPED (frozen governs §7); routing log aggregate {routing_mode, executed_at}, `pipeline_counts` NOT broken out (unenumerated sub-shape + implies-exclusion risk); deferral invisible (Doc-3 §4.2) — delivered-onward fixture + explicit "not-invited/deferred never shown" note; non-penalizing (declined/expired=neutral≠danger); R6 (no dispatch/select/exclude/re-rank); GI-04 contract order; GI-03 cursor no total; notFound byte-identical; opaque route id. VERBATIM: routing_mode (Doc-2:757), invitation states (Doc-4M:120). Shared edits git-verified ADDITIVE (+19 view-models, +21 state-display, 0 del). OBS (wiring): buyer InvitationState union carries pre-delivery states for type fidelity → server MUST enforce delivered-onward filter (consider buyer-only subtype). Reuses ActivityTimeline+DataListTable+shell/format/state-display |
| P-BUY-14 | Quotation detail | P1 | Ready | 🟩 Built | **FE-BUY-04 enhancement CLOSED** (RV-0102, `d5c09fe`): quoted-amount/valid-until/received promoted into a `KpiStatCard` band; header/sidebar duplication removed; version-history timeline relabeled; mobile a11y fix on the price table |
| P-BUY-15 | Comparison statement | P1 | Ready | 🟩 Built | read-only, System-gen, **never recommends** (R6). **FE-BUY-05 enhancement CLOSED** (RV-0108, `79b738a`): seeded a realistic comparison fixture (route previously always passed `suppliers: []`); R6-clean per both review lanes |
| P-BUY-16 | Clarifications / thread | P1 | Ready | 🟩 Built | **FE-BUY-04 enhancement CLOSED** (RV-0102, `5a4550c`): quotation-context cross-link from P-BUY-14 ("Ask a clarification"); fixture human-ref threaded for breadcrumb |
| P-BUY-17 | Award | P1 | Ready | ✅ Approved | RV-0005 PASS (re-review) — mock shortlist + working GET-form wizard nav (no default winner, R6); `<fieldset>/<legend>`. Both RV-0002 findings resolved. **FE-BUY-06 enhancement CLOSED** (RV-0109, `5654956`): fixture-consistency fix (aligned to the established RFQ-2026-000123 universe) + comparison cross-link |
| P-BUY-18 | Close lost | P2 | Ready | ✅ Approved | RV-0009 PASS (Team-4). reason_code enum VERBATIM (Doc-4E §E8.5); non-penalizing exemplary; notFound Inv #11; GET-form + loading.tsx. NIT: conditional-required not native-enforced. **FE-BUY-06 reviewed, carried forward untouched** (RV-0109) — no defect found |
| P-BUY-19 | Engagements | P1 | Ready | ✅ Approved | RV-0011 PASS (Team-4). Projection discipline exemplary — only the 3 frozen `list_engagements` fields; no free-text search; cursor pagination; genuine-empty (Inv #11). **FE-BUY-07 reviewed, carried forward untouched** (RV-0112) — no defect found |
| P-BUY-20 | Engagement detail | P1 | Ready | ✅ Approved | RV-0015 PASS (Team-4). EXEMPLARY — only projected `get_engagement` fields; currency-driven Money (BDT not assumed); counterparty opaque ref (no coined name); ENG-01/02/03 registered, in-code only; money-boundary; notFound byte-identical. Model detail page. **FE-BUY-07 enhancement CLOSED** (RV-0112, `2d1b23e`): the Documents card's permanent "later milestone" dead-end replaced with plain navigation to the 5 already-built document-kind routes (P-BUY-21..25) — one fix-and-reverify cycle (Review-A caught and the caption's per-engagement existence-claim wording was removed) |
| P-BUY-21 | Purchase order | P1 | Ready | ✅ Approved | RV-0022 PASS (Team-4). Projection verified verbatim vs Doc-4F §F5.8 (6 fields); PO body/total/list deliberately omitted (nothing coined); `can_approve_po` = real Doc-2 §7 slug, DISTINCT (never collapsed); money-boundary DF-6; notFound byte-identical, no leaf-ref leak; versioned Inv #8; loading.tsx; model detail page (on par with P-BUY-20). **FE-BUY-07 reviewed, carried forward untouched** (RV-0112) — no defect found; now linked from the Documents hub |
| P-BUY-22 | Payments | P1 | Ready | ✅ Approved | RV-0024 PASS (Team-4). `payment_records` projection verified verbatim vs Doc-2:783/Doc-4F §F5.6 {amount,currency,paid_at,method_note,status}; distinct slugs `can_record_payments`/`can_approve_payment` confirmed (Doc-4F §F5.6, "distinct"), Confirm only on `recorded`; money-boundary DF-6; notFound byte-identical; loading.tsx. Shared touches git-verified: `PaymentStatus`/`paymentStatusDisplay` additive, `stickyFirstColumn` pre-existing. Model detail page. **FE-BUY-07 reviewed, carried forward untouched** (RV-0112) — no defect found; now linked from the Documents hub |
| P-BUY-23 | Trade invoice review | P2 | Ready | ✅ Approved | RV-0028 PASS (Team-4). `trade_invoices` projection verified verbatim vs Doc-2:782/Doc-4F §F5.5 {human_ref,amount,currency,status,due_date}; ≠ platform_invoices (M4↔M7, DF-6) documented+rendered; status enum verbatim; NO coined "approve" (VM debunks page_inventory labels); dispute slug `can_record_payments` verified (§F5.5), `→disputed` emits DisputeRecorded server-side; additive `TradeInvoiceStatus`/`tradeInvoiceStatusDisplay` git-verified; notFound byte-identical; loading.tsx. Model detail page. **FE-BUY-07 reviewed, carried forward untouched** (RV-0112) — no defect found; now linked from the Documents hub |
| P-BUY-24 | Challan | P2 | Ready | ✅ Approved | RV-0031 PASS (Team-4). PO-parity (RV-0022) minus financial card; 6 `get_engagement_document` projected fields, doc_kind="challan" (frozen enum); READ-ONLY (`record_delivery`→DeliveryRecorded Trust input, no buyer write); no content_jsonb coined; versioned Inv#8; non-financial; notFound byte-identical; loading.tsx; ZERO shared-file edits. Doc-file card = promotion candidate at WCC. Model detail page. **FE-BUY-07 reviewed, carried forward untouched** (RV-0112) — no defect found; now linked from the Documents hub |
| P-BUY-25 | WCC | P2 | Ready | ✅ Approved | RV-0038 PASS (Team-4). WCC read-only versioned doc, 6 `get_engagement_document` fields, doc_kind=wcc (frozen enum), no content_jsonb coined; WorkCompletionIssued=Trust input; non-financial; notFound byte-identical; loading.tsx. **Shared `EngagementDocumentFileCard` extraction (rule of three) byte-equivalence GIT-VERIFIED by Team-4 — PO(RV-0022)/Challan(RV-0031) diffs = import prune + card swap only, exact copy strings preserved; both stay ✅.** axe green. **FE-BUY-07 reviewed, carried forward untouched** (RV-0112) — no defect found; now linked from the Documents hub |
| P-BUY-26 | CRM — vendor list | P2 | Ready | ✅ Approved | RV-0040 PASS (Team-4). NON-DISCLOSURE crux exact — `list_private_vendors.v1` projects ONLY {id,name,link_status,state} (Doc-4F §F4.9:466 verified); CRM approval status (approved/conditional/blacklisted) is a separate aggregate, NOT projected → NOT rendered → blacklist UNDETECTABLE (H.9/§7.5). link_status≠approval (called out); own-org only; genuine-empty; no free-text search (frozen link_status filter only); cursor pagination. Shared-state additions git-verified additive. Model non-disclosure listing. **FE-BUY-09 re-audited, CLOSED** (RV-0114, `adc84fa`, zero code delta) — blacklist-undetectability independently re-verified LIVE (blacklisted vs. approved list rows byte-identical); no safe change found |
| P-BUY-27 | CRM — vendor detail | P2 | Ready | ✅ Approved | RV-0043 PASS (Team-4). Non-disclosure handled exactly — projected fields only (§F4.9:466), `details_jsonb`/`caveat_note` omitted (not coined); `current_status` (approved/conditional/blacklisted/none) shown ONLY when linked + ONLY to owning buyer, explicit "never vendor-facing, never a platform score" framing; own-org `notFound()` byte-identical (H.9). DISTINCT slugs gated separately (`can_manage_vendor_status` vs `can_manage_private_vendors`, Doc-2:623/624); writes parked; shared-state additions git-verified additive. Completes buyer-private CRM pair — model non-disclosure surface. **P-BUY-1..27 buyer page set COMPLETE.** **FE-BUY-09 re-audited, CLOSED** (RV-0114, `adc84fa`, zero code delta) — DISTINCT-slugs + buyer-only-status framing both independently re-confirmed; forward-looking CRM↔Engagement cross-link candidate recorded, not built |

---

## Phase F1 — Buyer UX Refinement & Frontend Freeze (owner-directed 2026-07-02)

**Goal:** finish + freeze the ENTIRE buyer frontend *before* backend wiring. Presentation-only — **no**
backend/APIs/server-actions/DB/auth-wiring/mutations (owner-stated hard boundary). All frozen-corpus rules
still bind (§7): render only fields the frozen `create_rfq`/`update_rfq`/`submit_rfq` (Doc-4E) etc. project —
never coin; honor R6 (buyer never selects vendors), Inv#11 non-disclosure, GI-06 a11y. Contract-confirm each
deliverable first; Flag-and-Halt any owner-named section that would coin a field or break an invariant.

| ID | Deliverable | Priority | Status | Notes |
|---|---|---|---|---|
| F1-1 | **NEW RFQ Creation Experience** | P1 | ✅ Accepted as-is (owner 2026-07-02) | Grounding found the full 8-section flow ALREADY EXISTS as P-BUY-07 (`_components/rfq-create/*` + `rfqs/new/*`), fully contract-grounded (routing_mode not vendor-pick R6; Financial-Tier hint≠plan; WhatsApp non-disclosure). Owner reviewed + **chose "keep as-is → go to audits"** — no rebuild (honors F1-5 reuse rule). Existing single-page form + stepper indicator stands |
| F1-2 | **Buyer UX Polish** — end-to-end review | P2 | ✅ Audited | findings in FREEZE REPORT: FZ-01 container double-wrap (MAJOR, shared shell), FZ-03 hand-rolled h1 weight (MAJOR), + clean elsewhere (breadcrumbs/loading/empty/error/buttons/a11y) |
| F1-3 | **Buyer Forms Standardization** — audit | P2 | ✅ Audited | findings: FZ-04 RadioRow not consolidated (MAJOR), FZ-06 required-at-submit marking inconsistent (MAJOR), FZ-08 award legend required (MINOR), FZ-09 kit FormField role=alert (MINOR, cross-team), FZ-10 upload keyboard (MINOR) |
| F1-4 | **Mobile QA** — axe @390/768/1280 × 20 routes | P2 | ✅ Audited | only real own-content finding = FZ-02 not-found `<h1>` (MAJOR, a11y); rest = hydration flakes + known shell menu-trigger FP. axe otherwise clean |
| F1-5 | **Component Reuse Audit** | P2 | ✅ Audited | surface overwhelmingly clean (money/date/status/tables/lists/timelines all single-homed; no cross-surface leak). FZ-05 inline callout dup ~10× (MAJOR)→extract Callout; promotion candidates noted |
| F1-6 | **Frontend Freeze Report** | P2 | ✅ DELIVERED (v1.0) | `BUYER_FRONTEND_FREEZE_REPORT_v1.0.md`. Verdict: NOT-yet-frozen — BLOCKER 0 · MAJOR 6 · MINOR 5. Buyer-scoped fixes (FZ-02/03/04/05/06/08/10/11) = Team-2; cross-team (FZ-01 shell container, FZ-09 kit FormField) = escalate |

**OWNER SEQUENCING DECISION (2026-07-02):** **HOLD all freeze remediation** — do NOT apply FZ-01..FZ-11 yet.
Entering **Phase F2 — one final Buyer UX refinement pass**: implement the owner-provided additional business
information / cards / workflow improvements FIRST; then apply ALL freeze findings in a SINGLE remediation
pass; then run tsc + eslint + prettier + axe + the 2-review process before requesting freeze approval. The
Freeze Report is the parked known-issues ledger for that single batch.

## Phase F2 — Buyer Experience Enhancement Sprint (owner-directed 2026-07-02)

Owner goal: improve the Buyer module from a **business/procurement** perspective — identify missing buyer
info, improve dashboards + information hierarchy, add enterprise procurement widgets where appropriate,
improve RFQ/quotation presentation. **Presentation-only · reuse existing components · do NOT modify shared
platform components or design tokens · no backend/API.** Governance still binds: contract-backed only (no
coined fields, no client-computed figures — R7), R6/Inv#11/GI-* honored. **One page → Ready for Review →
STOP for review before the next.**

| ID | Deliverable | Priority | Status | Notes |
|---|---|---|---|---|
| BX-01 | **Dashboard enhancement** — populate the enterprise procurement dashboard (presentation fixture) + add **Sourcing Pipeline** widget (RFQ counts per frozen Doc-4M state; server-provided aggregate, never client-computed) + info hierarchy | P1 | ✅ Approved | RV-0070 PASS (Team-4). **No frozen-kit/token change** (git status src/frontend clean; composes kit Card+StatusChip). VM git-verified ADDITIVE (RfqPipelineStage + optional rfqPipeline?). Dashboard integration additive — first-run null path untouched, widget omitted when empty (no fabricated funnel). Frozen Doc-4M states via rfqStateDisplay (no coined); observe-only R6; counts server-provided never client-computed R7; GI-04 order; no excluded figure Inv#11. **OBS(wiring): per-state count = dashboard aggregate KPI over own bounded RFQs (like existing win-rate KPI), NOT a GI-03 paginated grand total; exact facet/count read (e.g. list_rfqs state facets) not pinned to a frozen contract → server-provided at wiring, coins no field.** axe 0 own-content |
| BX-02 | **RFQ detail presentation** — surface contract-backed procurement facts on the Overview (P-BUY-08) + info hierarchy | P1 | ✅ Approved | RV-0075 PASS (Team-4). **Inv#1 exemplary** — `work_nature[]` rendered as capability chip SET (never a label), values from frozen WORK_NATURE_OPTIONS. `routing_mode` observe-only (R6, display label, no re-rank/award); `current_version_no` + "Version history" cross-link → P-BUY-11 (Inv#8). Fields = frozen get_rfq content (Doc-2:757; content map-by-intent). No kit change (git status src/frontend clean; StatusChip/DescriptionList/Link); VM+options diffs additive; rows conditional (no fabricated value); tab structure preserved. SEED fixture presentation-only (was data=null), not-found path preserved (Inv#11), opaque id (Inv#5); quotation vendor names buyer-disclosed (submitted quotes, P-BUY-09). Queue advanced |
| BX-03 | **Dashboard identity + KPI polish** — `WelcomeBand` (shared identity seed), `KpiStatCard` icon/tone treatment, topbar search shortcut (`/discover` link-out) | P2 | ✅ Complete | RV-0133 (owner-direct edit `6316763` + Team-2 verification pass, dev-team self-close — small additive delta, same disposition class as other clean owner-authored deltas this session). tsc/eslint/prettier clean; live render `/dashboard` D/M 200; axe 0 desktop, 1 mobile OBS (pre-existing shell `quick-create` `button-name`, Board agenda #11, NOT this delta's defect — reproduces identically without these changes); `ShellViewModel.search` optional/additive, verified all 21 other shell-VM consumers (vendor/admin/account) unaffected and still render clean; `KpiStatCard` confirmed buyer-scoped only (no shared-kit primitive duplication — sole other consumer is `quotation-detail-view.tsx`, backward-compatible since `tone`/`icon` are optional) |
| BX-04 | **Buyer sidebar canonical Information Architecture** — full re-group (Dashboard/Procurement/Marketplace/Communication/Analytics/Organization/Account), 2-level nav groups (new additive `NavItem.children`), 6 scaffolded "Implementation Pending" routes, `Archived RFQs`→`Closed RFQs` rename | P1 | ✅ Complete | RV-0134, owner-directed in-session (verbatim 6-point directive: build complete sidebar, scaffold not omit missing pages, no fake data/logic, keep nav stable, reuse shell/PageHeader/EmptyState, rename Archived→Closed). Reuse-audited first: 8/24 leaves direct-reuse (incl. `/notifications`, previously built but never nav-mounted — Board agenda #8), 2 reuse `/rfqs` via a real frozen `draft` state filter, 4 already-shipped pages the mockup omitted (Approvals/Engagements/Documents/Vendor CRM) folded in rather than dropped (not authorized to remove), 8 leaves scaffolded via one new shared `ImplementationPendingView` (no fake data/filters — GI-12). Shared shell `NavItem.children?` extension additive/optional (BX-03 `search`-field precedent), `Sidebar`+`MobileNav` both updated. tsc/eslint/prettier clean; all 22 routes live 200; axe 0 desktop (1 pre-existing mobile OBS, Board agenda #11, not new); mobile drawer Playwright-verified full nested tree; vendor/admin regression-checked clean |
| BX-05+ | Further enhancements (quotation presentation, other widgets) | P2 | ⬜ | per owner, page-by-page after each review |
| F2-Z | Single freeze-remediation batch (all FZ-01..FZ-11) + full verify + 2-review → request freeze | P1 | ✅ Buyer-scoped portion Complete (`FE-CLN-01`, RV-0115, `636c192`) | FZ-02/03/04/05/06/08/10/11 fixed + verified (tsc/eslint/prettier/axe clean); A:PASS ∧ B:PASS, 0 B/M/M. FZ-01 (shell)/FZ-09 (kit FormField) remain cross-team, tracked at `FE-CLN-02`/`FE-DS-06` — buyer freeze verdict still pending those two |

**FE-BUY-10 (Discovery & Favorites) — checkpointed 2026-07-03, awaiting Review-A.** Owner resolved
both Board agenda #3 items live: P-BUY-03/04 route topology → reuse existing surfaces (no new
routes; see P-BUY-04 row); P-BUY-05 favorites scope → confirmed product/category, build stays
held on the projection gap. WP card:
`governanceReviews/milestones/fe-buy-10-discovery-favorites/WORK-PACKAGE.md`. **Still owner-gated:
none** — P-BUY-05 remains held on the target-resolution-read gap itself (not on a scope
decision).

## Documents — `P-DOC` buyer leg (FE-DOC track, WBS v1.2 — owner Board-minted 2026-07-03)

| ID | Title | Priority | Dependency | Status | Notes |
|---|---|---|---|---|---|
| P-DOC-01 | Buyer Documents hub | P1 | Ready (H: FE-DOC-00 delivered same pass) | ✅ Approved (RV-0129, A:PASS B:PASS, 0 B/M/M both lanes, `3293009`) | `/documents` — presentation composition of frozen M3/M4/M7 records; LifecycleStrip (**navigation, not state**), `?q=`/`?view=`/`?stage=` allowlisted presets, collapsed FilterSidebar (frozen facets, no Project), per-engagement timelines (restated facts), deep links only (never re-renders P-BUY-14/15/21..25); shared documents home (12 files) created here; opaque counterparty refs (P-BUY-20 precedent); ASYNC-pending + sharing-revoked honesty; DF-6 callout; walkthrough 7/7 + deep links 8/8 · 200. Frozen kinds ONLY — absent kinds = `esc_registry.md` §Document Management, never rendered. 1 pre-existing shared-shell a11y OBS carried forward to shell owner (quick-create mobile `button-name`). WP `governanceReviews/milestones/fe-doc-01-buyer-documents-hub/` |
