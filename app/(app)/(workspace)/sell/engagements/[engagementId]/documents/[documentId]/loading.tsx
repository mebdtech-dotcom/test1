// Skeleton-first loading for document detail (companion §7.2 pattern 1). Byte-equivalent (BE-6).
import { VendorContentSkeleton } from "../../../../../../_components/vendor";

export default function EngagementDocumentLoading() {
  return <VendorContentSkeleton />;
}
