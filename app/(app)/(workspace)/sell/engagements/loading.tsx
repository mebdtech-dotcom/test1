// Skeleton-first loading for the engagements index (companion §7.2 pattern 1). Byte-equivalent (BE-6).
import { VendorContentSkeleton } from "../../../_components/vendor";

export default function EngagementsLoading() {
  return <VendorContentSkeleton />;
}
