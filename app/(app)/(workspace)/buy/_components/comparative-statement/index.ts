// Comparative Statement (CS) — buyer host barrel (WP-2). The document view + view-models only;
// the stylesheet (`comparative-statement.css`) is imported by the route page.

export { ComparativeStatementView } from "./cs-document";
export type {
  ComparativeStatementData,
  CsApprovalBlock,
  CsBuyerEvaluation,
  CsComputed,
  CsLetterhead,
  CsLineCell,
  CsLineItem,
  CsVendor,
} from "./cs-view-models";
