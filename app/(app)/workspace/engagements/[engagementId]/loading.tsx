// Skeleton-first loading for engagement detail (companion §7.2 pattern 1). Byte-equivalent (BE-6).
import { VendorContentSkeleton } from "../../../_components/vendor";

export default function EngagementDetailLoading() {
  return <VendorContentSkeleton />;
}
