// Public landing route (`/`) — lives in the (public) route group
// (REPOSITORY_STRUCTURE §8). Spine-only placeholder; real marketing/discovery
// surfaces land with their owning Doc-7 wave.
export default function HomePage() {
  return (
    <main>
      <h1>iVendorz</h1>
      <p>Industrial Procurement Operating System — repository bootstrap (Wave 0).</p>
    </main>
  );
}
