# design/ — Design Source Files

**Charter.** Design **source assets only**: `figma/` exports, image `exports/`, raw `assets/`.
Owner: UX · mutable · registered by REPOSITORY_STRUCTURE.md v1.1 P-4.

Rule of separation: **`design/` holds sources; `docs/frontend/` holds engineering
documentation.** Token values, component conventions, and realization standards live in
`docs/frontend/` and the frozen Doc-7 series — never here.

Subdirectories materialize on first asset per the Reserved-Directory rule (`docs/README.md` §3).
